<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# presentation

```go
import "github.com/connerohnesorge/goffice/presentation"
```

Package presentation provides PresentationML support for PowerPoint documents.

This package implements the document\-level API for creating, reading, and modifying .pptx files \(and related formats like .potx, .ppsx, .pptm, .potm, .ppsm, .ppam\).

### Creating Documents

Create a new presentation at a file path:

```
doc, err := presentation.New("presentation.pptx", presentation.DocTypePresentation)
if err != nil {
	log.Fatal(err)
}
defer doc.Close()
```

Create different document types:

```
doc, _ := presentation.New("template.potx", presentation.DocTypeTemplate)
doc, _ := presentation.New("show.ppsx", presentation.DocTypeSlideshow)
doc, _ := presentation.New("macro.pptm", presentation.DocTypeMacroEnabledPresentation)
```

Write to an io.Writer:

```
var buf bytes.Buffer
doc, err := presentation.NewWriter(&buf, presentation.DocTypePresentation)
```

### Opening Documents

Open an existing presentation:

```
doc, err := presentation.Open("presentation.pptx", true) // editable=true
if err != nil {
	log.Fatal(err)
}
defer doc.Close()
```

Open read\-only:

```
doc, err := presentation.Open("presentation.pptx", false)
```

Open from an io.ReaderAt:

```
doc, err := presentation.OpenReader(reader, size, true)
```

### Accessing Document Content

Access the main presentation part:

```
presPart := doc.PresentationPart()
```

### Saving Documents

Save to the original location:

```
err := doc.Save()
```

Save to a new location:

```
err := doc.SaveAs("copy.pptx")
```

### Document Types

The package supports different PowerPoint document types:

- DocTypePresentation: Standard presentation \(.pptx\)
- DocTypeTemplate: Template \(.potx\)
- DocTypeSlideshow: Slideshow \(.ppsx\)
- DocTypeMacroEnabledPresentation: Macro\-enabled presentation \(.pptm\)
- DocTypeMacroEnabledTemplate: Macro\-enabled template \(.potm\)
- DocTypeMacroEnabledSlideshow: Macro\-enabled slideshow \(.ppsm\)
- DocTypeAddIn: Add\-in \(.ppam\)

Change document type:

```
err := doc.ChangeType(presentation.DocTypeSlideshow)
```

### Document Settings

Configure document open settings:

```
settings := &presentation.OpenSettings{
	AutoSave: true,
	MaxCharactersInPart: 0, // unlimited
}
doc, err := presentation.OpenWithSettings("doc.pptx", true, settings)
```

### Thread Safety

Document operations are protected by sync.RWMutex. Read operations can proceed concurrently, while write operations require exclusive access.

### Subpackages

The presentation package is organized into subpackages:

- presentation/elements: PresentationML element types
- presentation/parts: Document part types \(PresentationPart, SlidePart, etc.\)

See those packages for lower\-level element manipulation.

Package presentation provides PresentationML support for PowerPoint documents. This file implements the document\-level API for creating, reading, and modifying .pptx files.

<details><summary>Example (Animation)</summary>
<p>

Example\_animation demonstrates adding animations to a slide.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"anim.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in animation: %v",
				errClose,
			)
		}
	}()

	slidePart, errAdd := doc.AddSlide()
	if errAdd != nil {
		log.Printf(
			"failed to add slide in animation: %v",
			errAdd,
		)

		return
	}
	slide := slidePart.Slide()

	// Add a shape to animate
	shape := slide.AddShape()
	shape.NonVisualShapeProperties().SetId(2)

	// Add animation
	timing := slide.Timing()
	if timing == nil {
		// Timing is usually initialized when creating a slide if needed,
		// or we can add it.
		// For this example, we assume we can get or create it.
		// The current API might not expose a direct "Timing()" method on Slide if it's optional and not created by default.
		// But let's check what we saw in slide.go. It has Timing().
		log.Println("Timing not present")
	}

	// Actually slide.go has Timing() which returns *SlideTiming.
	// But it returns nil if not present.
	// We might need to construct it or use a helper if available.
	// Let's assume for this example we are just showing the intent.

	fmt.Println(
		"Animation example code structure",
	)
}
```

#### Output

```
Animation example code structure
```

</p>
</details>

<details><summary>Example (Chart Creation)</summary>
<p>

Example\_chartCreation demonstrates creating a chart on a slide.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"chart.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in chartCreation: %v",
				errClose,
			)
		}
	}()

	slidePart, errAdd := doc.AddSlide()
	if errAdd != nil {
		log.Printf(
			"failed to add slide in chartCreation: %v",
			errAdd,
		)

		return
	}
	slide := slidePart.Slide()

	// Add a graphic frame for the chart
	// Note: Full chart creation involves creating a ChartPart,
	// populating chart data, and linking it.
	shapeTree := slide.GetOrCreateShapeTree()
	gf := shapeTree.AddGraphicFrame()
	gf.SetPosition(1000, 1000)
	gf.SetSize(2000000, 2000000)

	fmt.Println("Created graphic frame for chart")
}
```

#### Output

```
Created graphic frame for chart
```

</p>
</details>

<details><summary>Example (Hello World)</summary>
<p>

Example\_helloWorld demonstrates creating a simple presentation with one slide.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	// Create a new presentation
	doc, err := presentation.New(
		"hello.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in helloWorld: %v",
				errClose,
			)
		}
	}()

	// Add a slide
	slidePart, err := doc.AddSlide()
	if err != nil {
		log.Printf("failed to add slide: %v", err)

		return
	}

	// Get the slide element
	slide := slidePart.Slide()
	if slide != nil {
		fmt.Println("Successfully added a slide")
	}

}
```

#### Output

```
Successfully added a slide
```

</p>
</details>

<details><summary>Example (Image Insertion)</summary>
<p>

Example\_imageInsertion demonstrates adding an image to a slide.

```go
package main

import (
	"fmt"
	"log"
	"os"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	// Create a dummy image file for demonstration
	if errWrite := os.WriteFile("image.png", []byte("dummy image content"), 0o644); errWrite != nil {
		log.Printf(
			"failed to write dummy image: %v",
			errWrite,
		)
	}
	defer func() {
		if errRemove := os.Remove("image.png"); errRemove != nil {
			log.Printf(
				"failed to remove dummy image: %v",
				errRemove,
			)
		}
	}()

	doc, err := presentation.New(
		"image.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in imageInsertion: %v",
				errClose,
			)
		}
	}()

	slidePart, errAdd := doc.AddSlide()
	if errAdd != nil {
		log.Printf(
			"failed to add slide in imageInsertion: %v",
			errAdd,
		)

		return
	}
	slide := slidePart.Slide()

	// Add image to slide
	// Note: In a real scenario, you would use a valid image file.
	// This adds a relationship to the image part and a picture element to the slide.
	pic := slide.AddPicture("rId1")
	if pic != nil {
		fmt.Println(
			"Added picture placeholder to slide",
		)
	}

	// Normally you would add the image part to the slide part:
	// imgPart, _ := slidePart.AddImagePart("image.png")
	// pic.BlipFill().Blip().SetEmbed(imgPart.RelationshipID())

}
```

#### Output

```
Added picture placeholder to slide
```

</p>
</details>

<details><summary>Example (Modify Presentation)</summary>
<p>

Example\_modifyPresentation demonstrates opening and modifying an existing presentation.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	// Create a dummy file first
	startDoc, errNew := presentation.New(
		"modify.pptx",
		presentation.DocTypePresentation,
	)
	if errNew != nil {
		log.Printf(
			"failed to create initial presentation in modifyPresentation: %v",
			errNew,
		)

		return
	}
	if _, errAdd1 := startDoc.AddSlide(); errAdd1 != nil {
		log.Printf(
			"failed to add slide to startDoc: %v",
			errAdd1,
		)
	}
	if errSave := startDoc.Save(); errSave != nil {
		log.Printf(
			"failed to save startDoc: %v",
			errSave,
		)
	}
	if errClose1 := startDoc.Close(); errClose1 != nil {
		log.Printf(
			"failed to close startDoc: %v",
			errClose1,
		)
	}

	// Open for editing
	doc, err := presentation.Open(
		"modify.pptx",
		true,
	)
	if err != nil {
		log.Printf(
			"failed to open presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose2 := doc.Close(); errClose2 != nil {
			log.Printf(
				"failed to close doc in modifyPresentation: %v",
				errClose2,
			)
		}
	}()

	// Add another slide
	if _, errAdd2 := doc.AddSlide(); errAdd2 != nil {
		log.Printf(
			"failed to add second slide in modifyPresentation: %v",
			errAdd2,
		)
	}

	fmt.Printf(
		"Slide count after modification: %d",
		doc.SlideCount(),
	)
}
```

#### Output

```
Slide count after modification: 2
```

</p>
</details>

<details><summary>Example (Slide Count)</summary>
<p>

Example\_slideCount demonstrates counting slides.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"count.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in slideCount: %v",
				errClose,
			)
		}
	}()

	if _, errAdd1 := doc.AddSlide(); errAdd1 != nil {
		log.Printf(
			"failed to add first slide: %v",
			errAdd1,
		)

		return
	}
	if _, errAdd2 := doc.AddSlide(); errAdd2 != nil {
		log.Printf(
			"failed to add second slide: %v",
			errAdd2,
		)

		return
	}

	fmt.Printf(
		"Slide count: %d",
		doc.SlideCount(),
	)
}
```

#### Output

```
Slide count: 2
```

</p>
</details>

<details><summary>Example (Table Creation)</summary>
<p>

Example\_tableCreation demonstrates creating a table on a slide.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"table.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in tableCreation: %v",
				errClose,
			)
		}
	}()

	slidePart, errAdd := doc.AddSlide()
	if errAdd != nil {
		log.Printf(
			"failed to add slide in tableCreation: %v",
			errAdd,
		)

		return
	}
	slide := slidePart.Slide()

	// Add a graphic frame for the table
	shapeTree := slide.GetOrCreateShapeTree()
	gf := shapeTree.AddGraphicFrame()

	// In a full implementation, you would set the graphic data to a table type
	// and add rows/columns.

	if gf != nil {
		fmt.Println(
			"Created graphic frame for table",
		)
	}
}
```

#### Output

```
Created graphic frame for table
```

</p>
</details>

<details><summary>Example (Text Formatting)</summary>
<p>

Example\_textFormatting demonstrates creating a slide with formatted text.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/drawingml"
	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"text.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in textFormatting: %v",
				errClose,
			)
		}
	}()

	slidePart, errAdd := doc.AddSlide()
	if errAdd != nil {
		log.Printf(
			"failed to add slide in textFormatting: %v",
			errAdd,
		)

		return
	}
	slide := slidePart.Slide()

	// Add a text box (rectangle shape with text)
	shape := slide.AddShape()

	// Set shape type to rectangle
	spPr := shape.GetOrCreateShapeProperties()
	spPr.SetPresetGeometry(
		string(drawingml.ShapeTypeRectangle),
	)

	// Add text
	tb := shape.GetOrCreateTextBody()
	p := tb.AddParagraph("Hello, ")

	// Add run with bold text
	run := p.AddRun("World!")
	run.SetBold(true)
	run.SetFontSize(2400) // 24pt

	fmt.Println("Created text formatted slide")
}
```

#### Output

```
Created text formatted slide
```

</p>
</details>

<details><summary>Example (Validation)</summary>
<p>

Example\_validation demonstrates validating a presentation.

```go
package main

import (
	"fmt"
	"log"

	"github.com/connerohnesorge/goffice/openxml/validation"
	"github.com/connerohnesorge/goffice/presentation"
)

func main() {
	doc, err := presentation.New(
		"valid.pptx",
		presentation.DocTypePresentation,
	)
	if err != nil {
		log.Printf(
			"failed to create presentation: %v",
			err,
		)

		return
	}
	defer func() {
		if errClose := doc.Close(); errClose != nil {
			log.Printf(
				"failed to close document in validation: %v",
				errClose,
			)
		}
	}()

	// Validate against Office 2016 standards
	errors := doc.Validate(validation.Office2016)

	if !errors.HasErrors() {
		fmt.Println("Document is valid")
	} else {
		fmt.Printf("Document has %d errors", len(errors))
	}
}
```

#### Output

```
Document is valid
```

</p>
</details>

## Index

- [Constants](<#constants>)
- [Variables](<#variables>)
- [func applyHTMLToTextBody\(tb \*drawingml.TextBody, n \*html.Node, relFn func\(string\) string\)](<#applyHTMLToTextBody>)
- [func getAudioFormatFromContentType\(contentType string\) string](<#getAudioFormatFromContentType>)
- [func getFormatFromContentType\(contentType string\) string](<#getFormatFromContentType>)
- [func getText\(n \*html.Node\) string](<#getText>)
- [func isAudioFormatSupported\(contentType string\) bool](<#isAudioFormatSupported>)
- [func isFormatSupported\(contentType string\) bool](<#isFormatSupported>)
- [func parseRowNodes\(n \*html.Node\) \[\]\*html.Node](<#parseRowNodes>)
- [type AddAudioOptions](<#AddAudioOptions>)
  - [func DefaultAddAudioOptions\(\) \*AddAudioOptions](<#DefaultAddAudioOptions>)
- [type AddVideoOptions](<#AddVideoOptions>)
  - [func DefaultAddVideoOptions\(\) \*AddVideoOptions](<#DefaultAddVideoOptions>)
- [type Audio](<#Audio>)
  - [func \(a \*Audio\) SetLoop\(loop bool\) \*Audio](<#Audio.SetLoop>)
  - [func \(a \*Audio\) SetMuted\(muted bool\) \*Audio](<#Audio.SetMuted>)
  - [func \(a \*Audio\) SetPosition\(x, y int\) \*Audio](<#Audio.SetPosition>)
  - [func \(a \*Audio\) SetSize\(w, h int\) \*Audio](<#Audio.SetSize>)
  - [func \(a \*Audio\) SetVolume\(vol int\) \*Audio](<#Audio.SetVolume>)
  - [func \(a \*Audio\) applyProps\(\)](<#Audio.applyProps>)
- [type AudioPartInfo](<#AudioPartInfo>)
  - [func GetAudioInfo\(audio parts.MediaPart\) \*AudioPartInfo](<#GetAudioInfo>)
- [type DocType](<#DocType>)
  - [func \(dt DocType\) ContentType\(\) string](<#DocType.ContentType>)
  - [func \(dt DocType\) Extension\(\) string](<#DocType.Extension>)
  - [func \(dt DocType\) String\(\) string](<#DocType.String>)
- [type Document](<#Document>)
  - [func CreateFromTemplate\(templatePath string\) \(\*Document, error\)](<#CreateFromTemplate>)
  - [func New\(path string, docType DocType\) \(\*Document, error\)](<#New>)
  - [func NewWriter\(w io.Writer, docType DocType\) \(\*Document, error\)](<#NewWriter>)
  - [func Open\(path string, editable bool\) \(\*Document, error\)](<#Open>)
  - [func OpenReader\(r io.ReaderAt, size int64, editable bool\) \(\*Document, error\)](<#OpenReader>)
  - [func OpenWithSettings\(path string, editable bool, settings \*OpenSettings\) \(\*Document, error\)](<#OpenWithSettings>)
  - [func \(d \*Document\) AddAudio\(slideIndex int, filePath string\) \(\*Audio, error\)](<#Document.AddAudio>)
  - [func \(d \*Document\) AddAudioFromBytes\(slideIndex int, data \[\]byte, filename string, opts \*AddAudioOptions\) \(parts.MediaPart, error\)](<#Document.AddAudioFromBytes>)
  - [func \(d \*Document\) AddAudioFromFile\(slideIndex int, filePath string, opts \*AddAudioOptions\) \(parts.MediaPart, error\)](<#Document.AddAudioFromFile>)
  - [func \(d \*Document\) AddAudioFromReader\(slideIndex int, reader io.Reader, filename string, opts \*AddAudioOptions\) \(parts.MediaPart, error\)](<#Document.AddAudioFromReader>)
  - [func \(d \*Document\) AddPresentationPart\(\) \(\*parts.PresentationPart, error\)](<#Document.AddPresentationPart>)
  - [func \(d \*Document\) AddSlide\(\) \(\*parts.SlidePart, error\)](<#Document.AddSlide>)
  - [func \(d \*Document\) AddSlideMaster\(\) \(\*parts.SlideMasterPart, error\)](<#Document.AddSlideMaster>)
  - [func \(d \*Document\) AddSlideWithLayout\(layout \*parts.SlideLayoutPart\) \(\*parts.SlidePart, error\)](<#Document.AddSlideWithLayout>)
  - [func \(d \*Document\) AddTheme\(\) \(\*parts.ThemePart, error\)](<#Document.AddTheme>)
  - [func \(d \*Document\) AddVideo\(slideIndex int, filePath string\) \(\*Video, error\)](<#Document.AddVideo>)
  - [func \(d \*Document\) AddVideoFromBytes\(slideIndex int, data \[\]byte, filename string, opts \*AddVideoOptions\) \(parts.MediaPart, error\)](<#Document.AddVideoFromBytes>)
  - [func \(d \*Document\) AddVideoFromFile\(slideIndex int, filePath string, opts \*AddVideoOptions\) \(parts.MediaPart, error\)](<#Document.AddVideoFromFile>)
  - [func \(d \*Document\) AddVideoFromReader\(slideIndex int, reader io.Reader, filename string, opts \*AddVideoOptions\) \(parts.MediaPart, error\)](<#Document.AddVideoFromReader>)
  - [func \(d \*Document\) ChangeType\(newType DocType\) error](<#Document.ChangeType>)
  - [func \(d \*Document\) Close\(\) error](<#Document.Close>)
  - [func \(d \*Document\) CoreProperties\(\) \*packaging.CoreProperties](<#Document.CoreProperties>)
  - [func \(d \*Document\) DetectAudioType\(data \[\]byte, filename string\) parts.AudioType](<#Document.DetectAudioType>)
  - [func \(d \*Document\) ExtendedProperties\(\) openxml.OpenXmlPart](<#Document.ExtendedProperties>)
  - [func \(d \*Document\) Features\(\) \*features.FeatureCollection](<#Document.Features>)
  - [func \(d \*Document\) GetAudios\(\) \[\]parts.MediaPart](<#Document.GetAudios>)
  - [func \(d \*Document\) GetSlide\(index int\) \(\*parts.SlidePart, error\)](<#Document.GetSlide>)
  - [func \(d \*Document\) GetVideos\(\) \[\]parts.MediaPart](<#Document.GetVideos>)
  - [func \(d \*Document\) ImportHTMLTable\(slideIndex int, htmlStr string\) \(\*Table, error\)](<#Document.ImportHTMLTable>)
  - [func \(d \*Document\) ImportHTMLText\(slideIndex int, x, y, w, h float64, htmlStr string\) \(\*elements.Shape, error\)](<#Document.ImportHTMLText>)
  - [func \(d \*Document\) IsEditable\(\) bool](<#Document.IsEditable>)
  - [func \(d \*Document\) IsValid\(version validation.FileFormatVersions\) bool](<#Document.IsValid>)
  - [func \(d \*Document\) Package\(\) \*openxml.OpenXmlPackage](<#Document.Package>)
  - [func \(d \*Document\) Path\(\) string](<#Document.Path>)
  - [func \(d \*Document\) PresentationPart\(\) \*parts.PresentationPart](<#Document.PresentationPart>)
  - [func \(d \*Document\) RemoveVideo\(video parts.MediaPart\) error](<#Document.RemoveVideo>)
  - [func \(d \*Document\) Save\(\) error](<#Document.Save>)
  - [func \(d \*Document\) SaveAs\(path string\) error](<#Document.SaveAs>)
  - [func \(d \*Document\) SaveTo\(w io.Writer\) error](<#Document.SaveTo>)
  - [func \(d \*Document\) Settings\(\) \*OpenSettings](<#Document.Settings>)
  - [func \(d \*Document\) SlideCount\(\) int](<#Document.SlideCount>)
  - [func \(d \*Document\) SlideMasters\(\) iter.Seq\[\*parts.SlideMasterPart\]](<#Document.SlideMasters>)
  - [func \(d \*Document\) Slides\(\) iter.Seq\[\*parts.SlidePart\]](<#Document.Slides>)
  - [func \(d \*Document\) Theme\(\) \*parts.ThemePart](<#Document.Theme>)
  - [func \(d \*Document\) Type\(\) DocType](<#Document.Type>)
  - [func \(d \*Document\) Validate\(version validation.FileFormatVersions\) validation.ValidationErrors](<#Document.Validate>)
  - [func \(d \*Document\) ValidateWithSettings\(version validation.FileFormatVersions, settings \*validation.ValidationSettings\) validation.ValidationErrors](<#Document.ValidateWithSettings>)
  - [func \(d \*Document\) detectDocumentType\(\)](<#Document.detectDocumentType>)
  - [func \(d \*Document\) initializeDocument\(\) error](<#Document.initializeDocument>)
- [type FileFormatVersion](<#FileFormatVersion>)
- [type MarkupCompatibilityProcessSettings](<#MarkupCompatibilityProcessSettings>)
- [type OpenSettings](<#OpenSettings>)
  - [func DefaultOpenSettings\(\) \*OpenSettings](<#DefaultOpenSettings>)
  - [func \(s \*OpenSettings\) WithAutoSave\(autoSave bool\) \*OpenSettings](<#OpenSettings.WithAutoSave>)
  - [func \(s \*OpenSettings\) WithMaxCharacters\(maxChars int64\) \*OpenSettings](<#OpenSettings.WithMaxCharacters>)
  - [func \(s \*OpenSettings\) WithTargetVersion\(version FileFormatVersion\) \*OpenSettings](<#OpenSettings.WithTargetVersion>)
- [type ProcessMode](<#ProcessMode>)
- [type Table](<#Table>)
  - [func NewTable\(slide \*elements.Slide, rows, cols int\) \*Table](<#NewTable>)
  - [func NewTableFromGraphicFrame\(gf \*elements.GraphicFrame\) \*Table](<#NewTableFromGraphicFrame>)
  - [func \(t \*Table\) AppendRow\(\) \*Table](<#Table.AppendRow>)
  - [func \(t \*Table\) ColumnCount\(\) int](<#Table.ColumnCount>)
  - [func \(t \*Table\) DeleteRow\(index int\) error](<#Table.DeleteRow>)
  - [func \(t \*Table\) DrawingMLTable\(\) \*drawtable.Table](<#Table.DrawingMLTable>)
  - [func \(t \*Table\) GetCell\(row, col int\) \(\*TableCell, error\)](<#Table.GetCell>)
  - [func \(t \*Table\) GetCellText\(row, col int\) \(string, error\)](<#Table.GetCellText>)
  - [func \(t \*Table\) GraphicFrame\(\) \*elements.GraphicFrame](<#Table.GraphicFrame>)
  - [func \(t \*Table\) InsertRow\(index int\) error](<#Table.InsertRow>)
  - [func \(t \*Table\) RowCount\(\) int](<#Table.RowCount>)
  - [func \(t \*Table\) SetBandedColumns\(value bool\)](<#Table.SetBandedColumns>)
  - [func \(t \*Table\) SetBandedRows\(value bool\)](<#Table.SetBandedRows>)
  - [func \(t \*Table\) SetCellText\(row, col int, text string\) error](<#Table.SetCellText>)
  - [func \(t \*Table\) SetColumnWidth\(col int, widthPt float64\) error](<#Table.SetColumnWidth>)
  - [func \(t \*Table\) SetFirstRow\(value bool\)](<#Table.SetFirstRow>)
  - [func \(t \*Table\) SetPosition\(xPt, yPt float64\)](<#Table.SetPosition>)
  - [func \(t \*Table\) SetSize\(widthPt, heightPt float64\)](<#Table.SetSize>)
- [type TableCell](<#TableCell>)
  - [func \(tc \*TableCell\) GetText\(\) string](<#TableCell.GetText>)
  - [func \(tc \*TableCell\) Properties\(\) \*drawtable.CellProperties](<#TableCell.Properties>)
  - [func \(tc \*TableCell\) SetText\(text string\)](<#TableCell.SetText>)
  - [func \(tc \*TableCell\) TextBody\(\) \*drawingml.TextBody](<#TableCell.TextBody>)
- [type Video](<#Video>)
  - [func \(v \*Video\) SetAutoStart\(auto bool\) \*Video](<#Video.SetAutoStart>)
  - [func \(v \*Video\) SetLoop\(loop bool\) \*Video](<#Video.SetLoop>)
  - [func \(v \*Video\) SetMuted\(muted bool\) \*Video](<#Video.SetMuted>)
  - [func \(v \*Video\) SetPosition\(x, y int\) \*Video](<#Video.SetPosition>)
  - [func \(v \*Video\) SetPoster\(img \*parts.ImagePart\) \*Video](<#Video.SetPoster>)
  - [func \(v \*Video\) SetSize\(w, h int\) \*Video](<#Video.SetSize>)
  - [func \(v \*Video\) SetVolume\(vol int\) \*Video](<#Video.SetVolume>)
  - [func \(v \*Video\) applyProps\(\)](<#Video.applyProps>)
- [type VideoInfo](<#VideoInfo>)
  - [func GetVideoInfo\(video parts.MediaPart\) \*VideoInfo](<#GetVideoInfo>)
- [type docError](<#docError>)
  - [func \(e docError\) Error\(\) string](<#docError.Error>)


## Constants

<a name="ContentTypePresentationMLPresentation"></a>Content types for PowerPoint documents.

```go
const (
    // ContentTypePresentationMLPresentation is the content type for a standard
    // PowerPoint presentation main part.
    ContentTypePresentationMLPresentation = "application/vnd.openxmlformats-officedocument.presentationml.presentation.main+xml"

    // ContentTypePresentationMLTemplate is the content type for a PowerPoint
    // template main part.
    ContentTypePresentationMLTemplate = "application/vnd.openxmlformats-officedocument.presentationml.template.main+xml"

    // ContentTypePresentationMLSlideshow is the content type for a PowerPoint
    // slideshow main part.
    ContentTypePresentationMLSlideshow = "application/vnd.openxmlformats-officedocument.presentationml.slideshow.main+xml"

    // ContentTypePresentationMLMacroEnabled is the content type for a
    // macro-enabled PowerPoint presentation main part.
    ContentTypePresentationMLMacroEnabled = "application/vnd.ms-powerpoint.presentation.macroEnabled.main+xml"

    // ContentTypePresentationMLMacroTemplate is the content type for a
    // macro-enabled PowerPoint template main part.
    ContentTypePresentationMLMacroTemplate = "application/vnd.ms-powerpoint.template.macroEnabled.main+xml"

    // ContentTypePresentationMLMacroSlideshow is the content type for a
    // macro-enabled PowerPoint slideshow main part.
    ContentTypePresentationMLMacroSlideshow = "application/vnd.ms-powerpoint.slideshow.macroEnabled.main+xml"

    // ContentTypePresentationMLAddIn is the content type for a PowerPoint add-in
    // main part.
    ContentTypePresentationMLAddIn = "application/vnd.ms-powerpoint.addin.macroEnabled.main+xml"

    // ContentTypeSlide is the content type for a slide.
    ContentTypeSlide = "application/vnd.openxmlformats-officedocument.presentationml.slide+xml"

    // ContentTypeSlideLayout is the content type for a slide layout.
    ContentTypeSlideLayout = "application/vnd.openxmlformats-officedocument.presentationml.slideLayout+xml"

    // ContentTypeSlideMaster is the content type for a slide master.
    ContentTypeSlideMaster = "application/vnd.openxmlformats-officedocument.presentationml.slideMaster+xml"

    // ContentTypeNotesMaster is the content type for a notes master.
    ContentTypeNotesMaster = "application/vnd.openxmlformats-officedocument.presentationml.notesMaster+xml"

    // ContentTypeNotesSlide is the content type for a notes slide.
    ContentTypeNotesSlide = "application/vnd.openxmlformats-officedocument.presentationml.notesSlide+xml"

    // ContentTypeHandoutMaster is the content type for a handout master.
    ContentTypeHandoutMaster = "application/vnd.openxmlformats-officedocument.presentationml.handoutMaster+xml"

    // ContentTypePresProps is the content type for presentation properties.
    ContentTypePresProps = "application/vnd.openxmlformats-officedocument.presentationml.presProps+xml"

    // ContentTypeViewProps is the content type for view properties.
    ContentTypeViewProps = "application/vnd.openxmlformats-officedocument.presentationml.viewProps+xml"

    // ContentTypeTableStyles is the content type for table styles.
    ContentTypeTableStyles = "application/vnd.openxmlformats-officedocument.presentationml.tableStyles+xml"

    // ContentTypeCommentAuthors is the content type for comment authors.
    ContentTypeCommentAuthors = "application/vnd.openxmlformats-officedocument.presentationml.commentAuthors+xml"

    // ContentTypeComments is the content type for comments.
    ContentTypePresentationComments = "application/vnd.openxmlformats-officedocument.presentationml.comments+xml"

    // ContentTypeTags is the content type for tags.
    ContentTypeTags = "application/vnd.openxmlformats-officedocument.presentationml.tags+xml"
)
```

<a name="RelationshipTypeSlide"></a>Relationship types for PowerPoint documents.

```go
const (
    // RelationshipTypeSlide is the relationship type for slides.
    RelationshipTypeSlide = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/slide"

    // RelationshipTypeSlideLayout is the relationship type for slide layouts.
    RelationshipTypeSlideLayout = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/slideLayout"

    // RelationshipTypeSlideMaster is the relationship type for slide masters.
    RelationshipTypeSlideMaster = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/slideMaster"

    // RelationshipTypeNotesMaster is the relationship type for notes master.
    RelationshipTypeNotesMaster = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/notesMaster"

    // RelationshipTypeNotesSlide is the relationship type for notes slides.
    RelationshipTypeNotesSlide = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/notesSlide"

    // RelationshipTypeHandoutMaster is the relationship type for handout master.
    RelationshipTypeHandoutMaster = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/handoutMaster"

    // RelationshipTypePresProps is the relationship type for presentation properties.
    RelationshipTypePresProps = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/presProps"

    // RelationshipTypeViewProps is the relationship type for view properties.
    RelationshipTypeViewProps = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/viewProps"

    // RelationshipTypeTableStyles is the relationship type for table styles.
    RelationshipTypeTableStyles = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/tableStyles"

    // RelationshipTypeCommentAuthors is the relationship type for comment authors.
    RelationshipTypeCommentAuthors = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/commentAuthors"

    // RelationshipTypeTags is the relationship type for tags.
    RelationshipTypeTags = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/tags"
)
```

<a name="errTableNil"></a>Table\-related constants.

```go
const (
    // Error messages.
    errTableNil = "table is nil"

    // Default table positioning in points.
    defaultTablePositionPt = 72  // 1 inch = 72 points
    defaultTableWidthPt    = 432 // 6 inches = 432 points
    defaultTableHeightPt   = 288 // 4 inches = 288 points
)
```

## Variables

<a name="ErrReadOnly"></a>Errors

```go
var (
    ErrReadOnly = docError(
        "document is read-only",
    )
    ErrNoPath = docError(
        "document has no file path",
    )
    ErrPackageNil = docError(
        "package is nil",
    )
    ErrPresentationPartExists = docError(
        "presentation part already exists",
    )
    ErrNoPresentationPart = docError(
        "no presentation part",
    )
    ErrPartExists = docError(
        "part already exists",
    )
    ErrPartNotFound = docError(
        "part not found",
    )
    ErrSlideIndexOutOfRange = docError(
        "slide index out of range",
    )
)
```

<a name="_"></a>Ensure Document implements io.Closer.

```go
var _ io.Closer = (*Document)(nil)
```

<a name="applyHTMLToTextBody"></a>
## func applyHTMLToTextBody

```go
func applyHTMLToTextBody(tb *drawingml.TextBody, n *html.Node, relFn func(string) string)
```



<a name="getAudioFormatFromContentType"></a>
## func getAudioFormatFromContentType

```go
func getAudioFormatFromContentType(contentType string) string
```

Helper function to get audio format name from content type.

<a name="getFormatFromContentType"></a>
## func getFormatFromContentType

```go
func getFormatFromContentType(contentType string) string
```

Helper function to get format name from content type.

<a name="getText"></a>
## func getText

```go
func getText(n *html.Node) string
```



<a name="isAudioFormatSupported"></a>
## func isAudioFormatSupported

```go
func isAudioFormatSupported(contentType string) bool
```

Helper function to check if audio format is supported.

<a name="isFormatSupported"></a>
## func isFormatSupported

```go
func isFormatSupported(contentType string) bool
```

Helper function to check if format is supported.

<a name="parseRowNodes"></a>
## func parseRowNodes

```go
func parseRowNodes(n *html.Node) []*html.Node
```



<a name="AddAudioOptions"></a>
## type AddAudioOptions

AddAudioOptions contains options for adding audio to a slide.

```go
type AddAudioOptions struct {
    StreamingThreshold int64
    ValidateFormat     bool
    AutoDetectType     bool
    AudioType          parts.AudioType
}
```

<a name="DefaultAddAudioOptions"></a>
### func DefaultAddAudioOptions

```go
func DefaultAddAudioOptions() *AddAudioOptions
```

DefaultAddAudioOptions returns default options for adding audio.

<a name="AddVideoOptions"></a>
## type AddVideoOptions

AddVideoOptions contains options for adding a video to a slide.

```go
type AddVideoOptions struct {
    // StreamingThreshold is the file size threshold for using streaming (default: 100MB).
    StreamingThreshold int64

    // ValidateFormat enables format validation (default: true).
    ValidateFormat bool

    // AutoDetectType enables automatic detection of video type from content (default: true).
    AutoDetectType bool

    // VideoType specifies the video type when auto-detection is disabled.
    VideoType parts.VideoType
}
```

<a name="DefaultAddVideoOptions"></a>
### func DefaultAddVideoOptions

```go
func DefaultAddVideoOptions() *AddVideoOptions
```

DefaultAddVideoOptions returns default options for adding videos.

<a name="Audio"></a>
## type Audio



```go
type Audio struct {
    slide *parts.SlidePart

    part parts.MediaPart

    pic *elements.Picture

    props elements.MediaProperties
}
```

<a name="Audio.SetLoop"></a>
### func \(\*Audio\) SetLoop

```go
func (a *Audio) SetLoop(loop bool) *Audio
```



<a name="Audio.SetMuted"></a>
### func \(\*Audio\) SetMuted

```go
func (a *Audio) SetMuted(muted bool) *Audio
```



<a name="Audio.SetPosition"></a>
### func \(\*Audio\) SetPosition

```go
func (a *Audio) SetPosition(x, y int) *Audio
```



<a name="Audio.SetSize"></a>
### func \(\*Audio\) SetSize

```go
func (a *Audio) SetSize(w, h int) *Audio
```



<a name="Audio.SetVolume"></a>
### func \(\*Audio\) SetVolume

```go
func (a *Audio) SetVolume(vol int) *Audio
```



<a name="Audio.applyProps"></a>
### func \(\*Audio\) applyProps

```go
func (a *Audio) applyProps()
```



<a name="AudioPartInfo"></a>
## type AudioPartInfo

AudioPartInfo contains information about an audio part.

```go
type AudioPartInfo struct {
    URI         string
    ContentType string
    Size        int64
    IsStreaming bool
    Format      string
    Supported   bool
}
```

<a name="GetAudioInfo"></a>
### func GetAudioInfo

```go
func GetAudioInfo(audio parts.MediaPart) *AudioPartInfo
```

GetAudioInfo returns information about an audio part.

<a name="DocType"></a>
## type DocType

DocType represents the type of a PowerPoint document.

```go
type DocType int
```

<a name="DocTypePresentation"></a>

```go
const (
    // DocTypePresentation represents a standard PowerPoint presentation (.pptx).
    DocTypePresentation DocType = iota

    // DocTypeTemplate represents a PowerPoint template (.potx).
    DocTypeTemplate

    // DocTypeSlideshow represents a PowerPoint slideshow (.ppsx).
    DocTypeSlideshow

    // DocTypeMacroEnabledPresentation represents a macro-enabled PowerPoint presentation (.pptm).
    DocTypeMacroEnabledPresentation

    // DocTypeMacroEnabledTemplate represents a macro-enabled PowerPoint template (.potm).
    DocTypeMacroEnabledTemplate

    // DocTypeMacroEnabledSlideshow represents a macro-enabled PowerPoint slideshow (.ppsm).
    DocTypeMacroEnabledSlideshow

    // DocTypeAddIn represents a PowerPoint add-in (.ppam).
    DocTypeAddIn
)
```

<a name="DocType.ContentType"></a>
### func \(DocType\) ContentType

```go
func (dt DocType) ContentType() string
```

ContentType returns the MIME content type for the main presentation part.

<a name="DocType.Extension"></a>
### func \(DocType\) Extension

```go
func (dt DocType) Extension() string
```

Extension returns the file extension for this document type.

<a name="DocType.String"></a>
### func \(DocType\) String

```go
func (dt DocType) String() string
```

String returns the string representation of the document type.

<a name="Document"></a>
## type Document

Document represents a PowerPoint presentation \(.pptx, .potx, .ppsx, .pptm, .potm, .ppsm, .ppam\). It wraps an OpenXmlPackage and provides PowerPoint\-specific functionality.

```go
type Document struct {
    // pkg is the underlying OpenXML package.
    pkg *openxml.OpenXmlPackage

    // docType is the type of this document.
    docType DocType

    // presentationPart is the cached main presentation part.
    presentationPart *parts.PresentationPart

    // settings are the options used to open/create this document.
    settings *OpenSettings

    // path is the file path (if opened from file).
    path string

    // isEditable indicates if the document was opened for editing.
    isEditable bool
}
```

<a name="CreateFromTemplate"></a>
### func CreateFromTemplate

```go
func CreateFromTemplate(templatePath string) (*Document, error)
```

CreateFromTemplate creates a new presentation from a template.

<a name="New"></a>
### func New

```go
func New(path string, docType DocType) (*Document, error)
```

New creates a new PowerPoint presentation at the specified path. The document type determines the file extension and content types.

<a name="NewWriter"></a>
### func NewWriter

```go
func NewWriter(w io.Writer, docType DocType) (*Document, error)
```

NewWriter creates a new PowerPoint presentation that writes to the given io.Writer.

<a name="Open"></a>
### func Open

```go
func Open(path string, editable bool) (*Document, error)
```

Open opens an existing PowerPoint presentation from the specified path. If editable is true, the document can be modified and saved.

<a name="OpenReader"></a>
### func OpenReader

```go
func OpenReader(r io.ReaderAt, size int64, editable bool) (*Document, error)
```

OpenReader opens a PowerPoint presentation from an io.ReaderAt.

<a name="OpenWithSettings"></a>
### func OpenWithSettings

```go
func OpenWithSettings(path string, editable bool, settings *OpenSettings) (*Document, error)
```

OpenWithSettings opens a PowerPoint presentation with custom settings.

<a name="Document.AddAudio"></a>
### func \(\*Document\) AddAudio

```go
func (d *Document) AddAudio(slideIndex int, filePath string) (*Audio, error)
```



<a name="Document.AddAudioFromBytes"></a>
### func \(\*Document\) AddAudioFromBytes

```go
func (d *Document) AddAudioFromBytes(slideIndex int, data []byte, filename string, opts *AddAudioOptions) (parts.MediaPart, error)
```

AddAudioFromBytes adds an audio file to the presentation from a byte slice.

<a name="Document.AddAudioFromFile"></a>
### func \(\*Document\) AddAudioFromFile

```go
func (d *Document) AddAudioFromFile(slideIndex int, filePath string, opts *AddAudioOptions) (parts.MediaPart, error)
```

AddAudioFromFile adds an audio file to the presentation.

<a name="Document.AddAudioFromReader"></a>
### func \(\*Document\) AddAudioFromReader

```go
func (d *Document) AddAudioFromReader(slideIndex int, reader io.Reader, filename string, opts *AddAudioOptions) (parts.MediaPart, error)
```

AddAudioFromReader adds an audio file to the presentation from an io.Reader.

<a name="Document.AddPresentationPart"></a>
### func \(\*Document\) AddPresentationPart

```go
func (d *Document) AddPresentationPart() (*parts.PresentationPart, error)
```

AddPresentationPart creates and adds the main presentation part. Returns an error if a main part already exists.

<a name="Document.AddSlide"></a>
### func \(\*Document\) AddSlide

```go
func (d *Document) AddSlide() (*parts.SlidePart, error)
```

AddSlide adds a new slide to the presentation. Returns the newly created SlidePart.

<a name="Document.AddSlideMaster"></a>
### func \(\*Document\) AddSlideMaster

```go
func (d *Document) AddSlideMaster() (*parts.SlideMasterPart, error)
```

AddSlideMaster adds a new slide master to the presentation. Returns the newly created SlideMasterPart.

<a name="Document.AddSlideWithLayout"></a>
### func \(\*Document\) AddSlideWithLayout

```go
func (d *Document) AddSlideWithLayout(layout *parts.SlideLayoutPart) (*parts.SlidePart, error)
```

AddSlideWithLayout adds a new slide to the presentation with the specified layout.

<a name="Document.AddTheme"></a>
### func \(\*Document\) AddTheme

```go
func (d *Document) AddTheme() (*parts.ThemePart, error)
```

AddTheme adds a theme to the presentation. Returns the newly created ThemePart.

<a name="Document.AddVideo"></a>
### func \(\*Document\) AddVideo

```go
func (d *Document) AddVideo(slideIndex int, filePath string) (*Video, error)
```



<a name="Document.AddVideoFromBytes"></a>
### func \(\*Document\) AddVideoFromBytes

```go
func (d *Document) AddVideoFromBytes(slideIndex int, data []byte, filename string, opts *AddVideoOptions) (parts.MediaPart, error)
```

AddVideoFromBytes adds a video to the presentation from a byte slice.

<a name="Document.AddVideoFromFile"></a>
### func \(\*Document\) AddVideoFromFile

```go
func (d *Document) AddVideoFromFile(slideIndex int, filePath string, opts *AddVideoOptions) (parts.MediaPart, error)
```

AddVideoFromFile adds a video to the presentation from a file path.

<a name="Document.AddVideoFromReader"></a>
### func \(\*Document\) AddVideoFromReader

```go
func (d *Document) AddVideoFromReader(slideIndex int, reader io.Reader, filename string, opts *AddVideoOptions) (parts.MediaPart, error)
```

AddVideoFromReader adds a video to the presentation from an io.Reader.

<a name="Document.ChangeType"></a>
### func \(\*Document\) ChangeType

```go
func (d *Document) ChangeType(newType DocType) error
```

ChangeType changes the document type. This updates the content type of the main presentation part.

<a name="Document.Close"></a>
### func \(\*Document\) Close

```go
func (d *Document) Close() error
```

Close closes the document and releases all resources. If AutoSave is enabled and the document has unsaved changes, it will be saved.

<a name="Document.CoreProperties"></a>
### func \(\*Document\) CoreProperties

```go
func (d *Document) CoreProperties() *packaging.CoreProperties
```

CoreProperties returns the core file properties part. Returns nil if not present.

<a name="Document.DetectAudioType"></a>
### func \(\*Document\) DetectAudioType

```go
func (d *Document) DetectAudioType(data []byte, filename string) parts.AudioType
```

DetectAudioType attempts to detect audio type from data or filename.

<a name="Document.ExtendedProperties"></a>
### func \(\*Document\) ExtendedProperties

```go
func (d *Document) ExtendedProperties() openxml.OpenXmlPart
```

ExtendedProperties returns the extended file properties part. Returns nil if not present.

<a name="Document.Features"></a>
### func \(\*Document\) Features

```go
func (d *Document) Features() *features.FeatureCollection
```

Features returns the document's feature collection.

<a name="Document.GetAudios"></a>
### func \(\*Document\) GetAudios

```go
func (d *Document) GetAudios() []parts.MediaPart
```

GetAudios returns all audio parts in the presentation.

<a name="Document.GetSlide"></a>
### func \(\*Document\) GetSlide

```go
func (d *Document) GetSlide(index int) (*parts.SlidePart, error)
```

GetSlide returns the slide at the specified index \(0\-based\). Returns an error if the index is out of range.

<a name="Document.GetVideos"></a>
### func \(\*Document\) GetVideos

```go
func (d *Document) GetVideos() []parts.MediaPart
```

GetVideos returns all video parts in the presentation.

<a name="Document.ImportHTMLTable"></a>
### func \(\*Document\) ImportHTMLTable

```go
func (d *Document) ImportHTMLTable(slideIndex int, htmlStr string) (*Table, error)
```

ImportHTMLTable imports an HTML table into the specified slide.

<a name="Document.ImportHTMLText"></a>
### func \(\*Document\) ImportHTMLText

```go
func (d *Document) ImportHTMLText(slideIndex int, x, y, w, h float64, htmlStr string) (*elements.Shape, error)
```

ImportHTMLText imports an HTML string into a text shape on the specified slide.

<a name="Document.IsEditable"></a>
### func \(\*Document\) IsEditable

```go
func (d *Document) IsEditable() bool
```

IsEditable returns true if the document was opened for editing.

<a name="Document.IsValid"></a>
### func \(\*Document\) IsValid

```go
func (d *Document) IsValid(version validation.FileFormatVersions) bool
```

IsValid returns true if the document has no validation errors for the specified version. This is a convenience method that calls Validate and checks if there are any errors.

<a name="Document.Package"></a>
### func \(\*Document\) Package

```go
func (d *Document) Package() *openxml.OpenXmlPackage
```

Package returns the underlying OpenXML package.

<a name="Document.Path"></a>
### func \(\*Document\) Path

```go
func (d *Document) Path() string
```

Path returns the file path of the document. Returns empty string if the document was created from a stream.

<a name="Document.PresentationPart"></a>
### func \(\*Document\) PresentationPart

```go
func (d *Document) PresentationPart() *parts.PresentationPart
```

PresentationPart returns the main presentation part. Returns nil if no main presentation part exists or the document is closed.

<a name="Document.RemoveVideo"></a>
### func \(\*Document\) RemoveVideo

```go
func (d *Document) RemoveVideo(video parts.MediaPart) error
```

RemoveVideo removes a video from the presentation.

<a name="Document.Save"></a>
### func \(\*Document\) Save

```go
func (d *Document) Save() error
```

Save saves the document to its original location. Returns an error if the document is read\-only or was created from a stream.

<a name="Document.SaveAs"></a>
### func \(\*Document\) SaveAs

```go
func (d *Document) SaveAs(path string) error
```

SaveAs saves the document to a new file path.

<a name="Document.SaveTo"></a>
### func \(\*Document\) SaveTo

```go
func (d *Document) SaveTo(w io.Writer) error
```

SaveTo writes the document to the given io.Writer. The document is written as a ZIP \(Office Open XML format\) containing all parts, relationships, and content types.

<a name="Document.Settings"></a>
### func \(\*Document\) Settings

```go
func (d *Document) Settings() *OpenSettings
```

Settings returns the open settings for this document.

<a name="Document.SlideCount"></a>
### func \(\*Document\) SlideCount

```go
func (d *Document) SlideCount() int
```

SlideCount returns the number of slides in the presentation.

<a name="Document.SlideMasters"></a>
### func \(\*Document\) SlideMasters

```go
func (d *Document) SlideMasters() iter.Seq[*parts.SlideMasterPart]
```

SlideMasters returns an iterator over all slide masters in the presentation.

<a name="Document.Slides"></a>
### func \(\*Document\) Slides

```go
func (d *Document) Slides() iter.Seq[*parts.SlidePart]
```

Slides returns an iterator over all slides in the presentation.

<a name="Document.Theme"></a>
### func \(\*Document\) Theme

```go
func (d *Document) Theme() *parts.ThemePart
```

Theme returns the first theme part of the presentation. Returns nil if no theme is present.

<a name="Document.Type"></a>
### func \(\*Document\) Type

```go
func (d *Document) Type() DocType
```

Type returns the document type.

<a name="Document.Validate"></a>
### func \(\*Document\) Validate

```go
func (d *Document) Validate(version validation.FileFormatVersions) validation.ValidationErrors
```

Validate validates the document structure against the specified Office version. It checks for schema compliance and semantic constraints. Returns validation errors if any issues are found, or an empty slice if valid.

<a name="Document.ValidateWithSettings"></a>
### func \(\*Document\) ValidateWithSettings

```go
func (d *Document) ValidateWithSettings(version validation.FileFormatVersions, settings *validation.ValidationSettings) validation.ValidationErrors
```

ValidateWithSettings validates the document with custom validation settings. This allows control over validation behavior such as maximum errors to collect, whether to validate semantics, and strict mode.

<a name="Document.detectDocumentType"></a>
### func \(\*Document\) detectDocumentType

```go
func (d *Document) detectDocumentType()
```

detectDocumentType determines the document type from the main part content type and wraps it as a PresentationPart.

<a name="Document.initializeDocument"></a>
### func \(\*Document\) initializeDocument

```go
func (d *Document) initializeDocument() error
```

initializeDocument sets up the basic document structure for a new document.

<a name="FileFormatVersion"></a>
## type FileFormatVersion

FileFormatVersion represents the target Office version.

```go
type FileFormatVersion int
```

<a name="FileFormatVersionOffice2007"></a>

```go
const (
    // FileFormatVersionOffice2007 targets Office 2007 format.
    FileFormatVersionOffice2007 FileFormatVersion = iota

    // FileFormatVersionOffice2010 targets Office 2010 format.
    FileFormatVersionOffice2010

    // FileFormatVersionOffice2013 targets Office 2013 format.
    FileFormatVersionOffice2013

    // FileFormatVersionOffice2016 targets Office 2016 format.
    FileFormatVersionOffice2016

    // FileFormatVersionOffice2019 targets Office 2019 format.
    FileFormatVersionOffice2019

    // FileFormatVersionOffice2021 targets Office 2021 format.
    FileFormatVersionOffice2021

    // FileFormatVersionMicrosoft365 targets Microsoft 365 format.
    FileFormatVersionMicrosoft365
)
```

<a name="MarkupCompatibilityProcessSettings"></a>
## type MarkupCompatibilityProcessSettings

MarkupCompatibilityProcessSettings controls markup compatibility processing.

```go
type MarkupCompatibilityProcessSettings struct {
    // ProcessModeValues determines which mc:ProcessContent elements to process.
    ProcessModeValues ProcessMode

    // TargetFileFormatVersions specifies which Office versions to target.
    TargetFileFormatVersions FileFormatVersion
}
```

<a name="OpenSettings"></a>
## type OpenSettings

OpenSettings contains options for opening or creating presentations.

```go
type OpenSettings struct {
    // AutoSave determines if the document should be automatically saved
    // when Close() is called and there are unsaved changes.
    AutoSave bool

    // MaxCharactersInPart sets the maximum number of characters allowed
    // in a single part. A value of 0 means no limit.
    MaxCharactersInPart int64

    // MarkupCompatibilityProcessSettings controls how markup compatibility
    // elements are processed. Currently not implemented.
    MarkupCompatibilityProcessSettings MarkupCompatibilityProcessSettings
}
```

<a name="DefaultOpenSettings"></a>
### func DefaultOpenSettings

```go
func DefaultOpenSettings() *OpenSettings
```

DefaultOpenSettings returns the default settings for opening presentations.

<a name="OpenSettings.WithAutoSave"></a>
### func \(\*OpenSettings\) WithAutoSave

```go
func (s *OpenSettings) WithAutoSave(autoSave bool) *OpenSettings
```

WithAutoSave returns a copy of the settings with AutoSave enabled.

<a name="OpenSettings.WithMaxCharacters"></a>
### func \(\*OpenSettings\) WithMaxCharacters

```go
func (s *OpenSettings) WithMaxCharacters(maxChars int64) *OpenSettings
```

WithMaxCharacters returns a copy of the settings with the specified max.

<a name="OpenSettings.WithTargetVersion"></a>
### func \(\*OpenSettings\) WithTargetVersion

```go
func (s *OpenSettings) WithTargetVersion(version FileFormatVersion) *OpenSettings
```

WithTargetVersion returns a copy of the settings with the target version.

<a name="ProcessMode"></a>
## type ProcessMode

ProcessMode specifies how to process markup compatibility content.

```go
type ProcessMode int
```

<a name="ProcessModeNoProcess"></a>

```go
const (
    // ProcessModeNoProcess does not process mc:ProcessContent.
    ProcessModeNoProcess ProcessMode = iota

    // ProcessModeProcessLoadedPartsOnly processes only loaded parts.
    ProcessModeProcessLoadedPartsOnly

    // ProcessModeProcessAllParts processes all parts.
    ProcessModeProcessAllParts
)
```

<a name="Table"></a>
## type Table

Table represents a high\-level table in a presentation slide. It wraps a GraphicFrame containing a DrawingML table.

```go
type Table struct {
    graphicFrame *elements.GraphicFrame
    table        *drawtable.Table
}
```

<a name="NewTable"></a>
### func NewTable

```go
func NewTable(slide *elements.Slide, rows, cols int) *Table
```

NewTable creates a new table and adds it to the slide with the specified number of rows and columns. This is a convenience function that can be called from user code.

<a name="NewTableFromGraphicFrame"></a>
### func NewTableFromGraphicFrame

```go
func NewTableFromGraphicFrame(gf *elements.GraphicFrame) *Table
```

NewTableFromGraphicFrame wraps an existing GraphicFrame that contains a table. This is used for reading existing tables from documents.

<a name="Table.AppendRow"></a>
### func \(\*Table\) AppendRow

```go
func (t *Table) AppendRow() *Table
```

AppendRow appends a new row to the table.

<a name="Table.ColumnCount"></a>
### func \(\*Table\) ColumnCount

```go
func (t *Table) ColumnCount() int
```

ColumnCount returns the number of columns in the table.

<a name="Table.DeleteRow"></a>
### func \(\*Table\) DeleteRow

```go
func (t *Table) DeleteRow(index int) error
```

DeleteRow deletes the row at the specified index.

<a name="Table.DrawingMLTable"></a>
### func \(\*Table\) DrawingMLTable

```go
func (t *Table) DrawingMLTable() *drawtable.Table
```

DrawingMLTable returns the underlying DrawingML table element.

<a name="Table.GetCell"></a>
### func \(\*Table\) GetCell

```go
func (t *Table) GetCell(row, col int) (*TableCell, error)
```

GetCell returns the cell at the specified row and column \(0\-based\).

<a name="Table.GetCellText"></a>
### func \(\*Table\) GetCellText

```go
func (t *Table) GetCellText(row, col int) (string, error)
```

GetCellText returns the text of the cell at the specified row and column.

<a name="Table.GraphicFrame"></a>
### func \(\*Table\) GraphicFrame

```go
func (t *Table) GraphicFrame() *elements.GraphicFrame
```

GraphicFrame returns the underlying GraphicFrame element.

<a name="Table.InsertRow"></a>
### func \(\*Table\) InsertRow

```go
func (t *Table) InsertRow(index int) error
```

InsertRow inserts a new row at the specified index.

<a name="Table.RowCount"></a>
### func \(\*Table\) RowCount

```go
func (t *Table) RowCount() int
```

RowCount returns the number of rows in the table.

<a name="Table.SetBandedColumns"></a>
### func \(\*Table\) SetBandedColumns

```go
func (t *Table) SetBandedColumns(value bool)
```

SetBandedColumns sets whether columns should be banded.

<a name="Table.SetBandedRows"></a>
### func \(\*Table\) SetBandedRows

```go
func (t *Table) SetBandedRows(value bool)
```

SetBandedRows sets whether rows should be banded.

<a name="Table.SetCellText"></a>
### func \(\*Table\) SetCellText

```go
func (t *Table) SetCellText(row, col int, text string) error
```

SetCellText sets the text of the cell at the specified row and column.

<a name="Table.SetColumnWidth"></a>
### func \(\*Table\) SetColumnWidth

```go
func (t *Table) SetColumnWidth(col int, widthPt float64) error
```

SetColumnWidth sets the width of the specified column in points.

<a name="Table.SetFirstRow"></a>
### func \(\*Table\) SetFirstRow

```go
func (t *Table) SetFirstRow(value bool)
```

SetFirstRow sets whether the first row should be styled specially.

<a name="Table.SetPosition"></a>
### func \(\*Table\) SetPosition

```go
func (t *Table) SetPosition(xPt, yPt float64)
```

SetPosition sets the position of the table in points.

<a name="Table.SetSize"></a>
### func \(\*Table\) SetSize

```go
func (t *Table) SetSize(widthPt, heightPt float64)
```

SetSize sets the size of the table in points.

<a name="TableCell"></a>
## type TableCell

TableCell represents a cell in a presentation table.

```go
type TableCell struct {
    cell *drawtable.TableCell
}
```

<a name="TableCell.GetText"></a>
### func \(\*TableCell\) GetText

```go
func (tc *TableCell) GetText() string
```

GetText returns the text content of the cell.

<a name="TableCell.Properties"></a>
### func \(\*TableCell\) Properties

```go
func (tc *TableCell) Properties() *drawtable.CellProperties
```

Properties returns the cell properties for advanced styling.

<a name="TableCell.SetText"></a>
### func \(\*TableCell\) SetText

```go
func (tc *TableCell) SetText(text string)
```

SetText sets the text content of the cell.

<a name="TableCell.TextBody"></a>
### func \(\*TableCell\) TextBody

```go
func (tc *TableCell) TextBody() *drawingml.TextBody
```

TextBody returns the underlying DrawingML TextBody for advanced formatting.

<a name="Video"></a>
## type Video



```go
type Video struct {
    slide *parts.SlidePart

    part parts.MediaPart

    pic *elements.Picture

    props elements.MediaProperties
}
```

<a name="Video.SetAutoStart"></a>
### func \(\*Video\) SetAutoStart

```go
func (v *Video) SetAutoStart(auto bool) *Video
```



<a name="Video.SetLoop"></a>
### func \(\*Video\) SetLoop

```go
func (v *Video) SetLoop(loop bool) *Video
```



<a name="Video.SetMuted"></a>
### func \(\*Video\) SetMuted

```go
func (v *Video) SetMuted(muted bool) *Video
```



<a name="Video.SetPosition"></a>
### func \(\*Video\) SetPosition

```go
func (v *Video) SetPosition(x, y int) *Video
```



<a name="Video.SetPoster"></a>
### func \(\*Video\) SetPoster

```go
func (v *Video) SetPoster(img *parts.ImagePart) *Video
```



<a name="Video.SetSize"></a>
### func \(\*Video\) SetSize

```go
func (v *Video) SetSize(w, h int) *Video
```



<a name="Video.SetVolume"></a>
### func \(\*Video\) SetVolume

```go
func (v *Video) SetVolume(vol int) *Video
```



<a name="Video.applyProps"></a>
### func \(\*Video\) applyProps

```go
func (v *Video) applyProps()
```



<a name="VideoInfo"></a>
## type VideoInfo

VideoInfo contains information about a video.

```go
type VideoInfo struct {
    URI         string
    ContentType string
    Size        int64
    IsStreaming bool
    Format      string
    Supported   bool
}
```

<a name="GetVideoInfo"></a>
### func GetVideoInfo

```go
func GetVideoInfo(video parts.MediaPart) *VideoInfo
```

GetVideoInfo returns information about a video.

<a name="docError"></a>
## type docError



```go
type docError string
```

<a name="docError.Error"></a>
### func \(docError\) Error

```go
func (e docError) Error() string
```



Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
