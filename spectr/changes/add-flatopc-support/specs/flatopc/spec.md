## ADDED Requirements

### Requirement: Flat OPC Support
The system SHALL support Flat OPC (Open Packaging Conventions) format, enabling documents to be stored as single XML files.

#### Scenario: Save as Flat OPC
- WHEN a document is saved in Flat OPC format
- THEN the output SHALL be a valid single XML file representation
