<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# wordprocessing

```go
import "github.com/connerohnesorge/goffice/wordprocessing"
```

Package wordprocessing provides WordprocessingML support for Word documents.

This package implements the document\-level API for creating, reading, and modifying .docx files \(and related formats like .dotx, .docm, .dotm\).

### Creating Documents

Create a new document at a file path:

```
doc, err := wordprocessing.New("document.docx", wordprocessing.DocTypeDocument)
if err != nil {
	log.Fatal(err)
}
defer doc.Close()
```

Create different document types:

```
doc, _ := wordprocessing.New("template.dotx", wordprocessing.DocTypeTemplate)
doc, _ := wordprocessing.New("macro.docm", wordprocessing.DocTypeMacroEnabled)
```

Write to an io.Writer:

```
var buf bytes.Buffer
doc, err := wordprocessing.NewWriter(&buf, wordprocessing.DocTypeDocument)
```

### Opening Documents

Open an existing document:

```
doc, err := wordprocessing.Open("document.docx", true) // editable=true
if err != nil {
	log.Fatal(err)
}
defer doc.Close()
```

Open read\-only:

```
doc, err := wordprocessing.Open("document.docx", false)
```

Open from an io.ReaderAt:

```
doc, err := wordprocessing.OpenReader(reader, size, true)
```

### Using the Builder Pattern

For creating documents programmatically, use the DocumentBuilder:

```
doc, err := wordprocessing.NewDocumentBuilder().
	AddHeading("My Document", 1).
	AlignCenter().
	Document().
	AddParagraph("Introduction text.").
	SpacingAfter(12).
	Document().
	AddTable(3, 4).
	SetCellText(0, 0, "Header").
	Document().
	Build()
```

### Accessing Document Content

Access the main document part:

```
mainPart := doc.MainPart()
docElement := mainPart.Document()
body := docElement.Body()
```

Iterate over paragraphs:

```
for p := range body.Paragraphs() {
	text := p.InnerText()
	fmt.Println(text)
}
```

Iterate over tables:

```
for table := range body.Tables() {
	for row := range table.Rows() {
		for cell := range row.Cells() {
			fmt.Println(cell.InnerText())
		}
	}
}
```

### Headers and Footers

Add headers and footers:

```
header, err := doc.AddHeader(elements.HeaderFooterDefault)
header.AppendParagraph("Header text")

footer, err := doc.AddFooter(elements.HeaderFooterDefault)
footer.AppendParagraph("Page ")
```

Iterate over headers/footers:

```
for header := range doc.Headers() {
	// process header
}
```

### Comments, Footnotes, Endnotes

Add comments:

```
comment, err := doc.AddComment("Author Name", "Comment text")
```

Access footnotes and endnotes:

```
fnPart, err := doc.FootnotesPart()
footnote := fnPart.AddFootnote("Footnote text")

enPart, err := doc.EndnotesPart()
endnote := enPart.AddEndnote("Endnote text")
```

### Saving Documents

Save to the original location:

```
err := doc.Save()
```

Save to a new location:

```
err := doc.SaveAs("copy.docx")
```

### Document Types

The package supports different Word document types:

- DocTypeDocument: Standard document \(.docx\)
- DocTypeTemplate: Template \(.dotx\)
- DocTypeMacroEnabled: Macro\-enabled document \(.docm\)
- DocTypeMacroTemplate: Macro\-enabled template \(.dotm\)

Change document type:

```
err := doc.ChangeType(wordprocessing.DocTypeTemplate)
```

### Document Settings

Configure document open settings:

```
settings := &wordprocessing.OpenSettings{
	AutoSave: true,
	MaxCharactersInPart: 0, // unlimited
}
doc, err := wordprocessing.OpenWithSettings("doc.docx", true, settings)
```

### Thread Safety

Document operations are protected by sync.RWMutex. Read operations can proceed concurrently, while write operations require exclusive access.

### Subpackages

The wordprocessing package is organized into subpackages:

- wordprocessing/elements: WordprocessingML element types
- wordprocessing/parts: Document part types \(MainPart, StylesPart, etc.\)

See those packages for lower\-level element manipulation.

Package wordprocessing provides APIs for creating and manipulating Word documents.

Comparison and Merge:

The package includes DocumentComparator and DocumentMerger for comparing and merging entire Word documents.

```
doc1, _ := wordprocessing.Open("base.docx", false)
doc2, _ := wordprocessing.Open("modified.docx", false)

comparator := wordprocessing.NewDocumentComparator()
diffs, _ := comparator.Compare(doc1, doc2)

merger := wordprocessing.NewDocumentMerger()
merger.Merge(doc1, doc2) // Merges doc2 into doc1
```

This package implements the document\-level API for creating, reading, and modifying .docx files.

Package wordprocessing implements the document\-level API for creating, reading, and modifying .docx files.

<details><summary>Example (Add Table)</summary>
<p>

Example\_addTable demonstrates creating tables in documents.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	// Add a title
	builder.AddParagraph("Sales Report").
		Bold().
		AlignCenter()

	// Create a 4x3 table (4 rows, 3 columns)
	builder.AddTable(4, 3).
		SetCellText(0, 0, "Product").
		SetCellText(0, 1, "Quantity").
		SetCellText(0, 2, "Price").
		SetCellText(1, 0, "Widget A").
		SetCellText(1, 1, "100").
		SetCellText(1, 2, "$10.00").
		SetCellText(2, 0, "Widget B").
		SetCellText(2, 1, "50").
		SetCellText(2, 2, "$20.00").
		SetCellText(3, 0, "Widget C").
		SetCellText(3, 1, "75").
		SetCellText(3, 2, "$15.00").
		SetStyle("TableGrid").
		SetWidthPercent(100)

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	// Verify table exists
	body := doc.Body()
	tableCount := 0
	for table := range body.Tables() {
		tableCount++
		fmt.Printf(
			"Table has %d rows\n",
			table.RowCount(),
		)
	}

	fmt.Printf(
		"Document has %d table(s)\n",
		tableCount,
	)
}
```

#### Output

```
Table has 4 rows
Document has 1 table(s)
```

</p>
</details>

<details><summary>Example (Build To Bytes)</summary>
<p>

Example\_buildToBytes demonstrates building to bytes.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()
	builder.AddParagraph(
		"Simple document content",
	)

	data, err := builder.BuildToBytes()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	// Output will vary based on XML size, but should be non-zero
	if len(data) > 0 {
		fmt.Println("XML generation successful")
	}
}
```

#### Output

```
XML generation successful
```

</p>
</details>

<details><summary>Example (Create Document)</summary>
<p>

Example\_createDocument demonstrates basic document creation.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	// Create a new document using the builder
	builder := wordprocessing.NewDocumentBuilder()

	// Add a title
	builder.AddParagraph("My First Document").
		Bold().
		FontSize(18).
		AlignCenter()

	// Add body content
	builder.AddParagraph(
		"This is the first paragraph of the document.",
	)
	builder.AddParagraph(
		"This is the second paragraph.",
	)

	// Build the document
	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	// Access the body and count paragraphs
	body := doc.Body()
	count := 0
	for range body.Paragraphs() {
		count++
	}

	fmt.Printf(
		"Document created with %d paragraphs\n",
		count,
	)
}
```

#### Output

```
Document created with 3 paragraphs
```

</p>
</details>

<details><summary>Example (Document Builder)</summary>
<p>

Example\_documentBuilder demonstrates using the fluent builder API.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	// Use method chaining to build a document
	doc, err := wordprocessing.NewDocumentBuilder().
		AddHeading("Report Title", 1).
		AlignCenter().
		Document().
		AddHeading("Introduction", 2).
		Document().
		AddParagraph("This report covers important topics.").
		SpacingAfter(12).
		Document().
		AddHeading("Section 1", 2).
		Document().
		AddParagraph("Content for section 1.").
		Document().
		Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	// Count elements
	body := doc.Body()
	paraCount := 0
	for range body.Paragraphs() {
		paraCount++
	}

	fmt.Printf(
		"Built document with %d paragraphs\n",
		paraCount,
	)
}
```

#### Output

```
Built document with 5 paragraphs
```

</p>
</details>

<details><summary>Example (Formatting)</summary>
<p>

Example\_formatting demonstrates text formatting options.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
	"github.com/connerohnesorge/goffice/wordprocessing/elements"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	// Create a paragraph with mixed formatting
	pb := builder.AddParagraph("")

	// Add runs with different formatting
	pb.AddRun("Normal text, ")
	pb.AddRun("bold text, ").Bold()
	pb.AddRun("italic text, ").Italic()
	pb.AddRun("underlined text, ").Underline()
	pb.AddRun("and ").Font("Arial")
	pb.AddRun("colored text.").Color("FF0000")

	// Add a paragraph with custom font
	builder.AddParagraph("This uses Times New Roman at 14pt.").
		Font("Times New Roman", 14)

	// Add a highlighted paragraph
	builder.AddParagraph("This text is highlighted.").
		Highlight(elements.HighlightYellow)

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	count := 0
	for range body.Paragraphs() {
		count++
	}
	fmt.Printf(
		"Created %d paragraphs with formatting\n",
		count,
	)
}
```

#### Output

```
Created 3 paragraphs with formatting
```

</p>
</details>

<details><summary>Example (Headers Footers)</summary>
<p>

Example\_headersFooters demonstrates adding headers and footers.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing/elements"
)

func main() {
	// Create header and footer elements directly
	header := elements.NewHeader()
	header.AppendParagraph("Document Header")

	footer := elements.NewFooter()
	footer.AppendParagraph("Page Footer")

	// Verify content
	headerParas := 0
	for range header.Paragraphs() {
		headerParas++
	}

	footerParas := 0
	for range footer.Paragraphs() {
		footerParas++
	}

	fmt.Printf(
		"Header has %d paragraph(s)\n",
		headerParas,
	)
	fmt.Printf(
		"Footer has %d paragraph(s)\n",
		footerParas,
	)
}
```

#### Output

```
Header has 1 paragraph(s)
Footer has 1 paragraph(s)
```

</p>
</details>

<details><summary>Example (Page Breaks)</summary>
<p>

Example\_pageBreaks demonstrates adding page breaks.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	builder.AddParagraph("Content on page 1")
	builder.AddPageBreak()
	builder.AddParagraph("Content on page 2")
	builder.AddPageBreak()
	builder.AddParagraph("Content on page 3")

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	count := 0
	for range body.Paragraphs() {
		count++
	}
	// Each page break adds a paragraph, so we have:
	// Page 1 content (1) + break para (1) + Page 2 content (1) + break para (1) + Page 3 content (1) = 5
	fmt.Printf(
		"Document has %d paragraphs (including page breaks)\n",
		count,
	)
}
```

#### Output

```
Document has 5 paragraphs (including page breaks)
```

</p>
</details>

<details><summary>Example (Paragraph Alignment)</summary>
<p>

Example\_paragraphAlignment demonstrates paragraph alignment options.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	builder.AddParagraph("Left aligned (default)").
		AlignLeft()
	builder.AddParagraph("Center aligned").
		AlignCenter()
	builder.AddParagraph("Right aligned").
		AlignRight()
	builder.AddParagraph("Justified text - this would normally be a longer paragraph to show the effect.").
		AlignJustify()

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	i := 0
	alignments := []string{
		"left",
		"center",
		"right",
		"justify",
	}
	for p := range body.Paragraphs() {
		if i < len(alignments) {
			props := p.Properties()
			if props != nil {
				fmt.Printf(
					"Paragraph %d: %s alignment\n",
					i+1,
					alignments[i],
				)
			}
		}
		i++
	}
}
```

#### Output

```
Paragraph 1: left alignment
Paragraph 2: center alignment
Paragraph 3: right alignment
Paragraph 4: justify alignment
```

</p>
</details>

<details><summary>Example (Paragraph Spacing)</summary>
<p>

Example\_paragraphSpacing demonstrates paragraph spacing and indentation.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	// Add paragraphs with custom spacing
	builder.AddParagraph("First paragraph with space after.").
		SpacingAfter(24)

		// 24 points

	builder.AddParagraph("Second paragraph with space before and after.").
		SpacingBefore(12).
		SpacingAfter(12)

	builder.AddParagraph("Indented paragraph.").
		LeftIndent(36) // 36 points = 0.5 inch

	builder.AddParagraph("First line indent.").
		FirstLineIndent(36)

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	count := 0
	for range body.Paragraphs() {
		count++
	}
	fmt.Printf(
		"Created %d paragraphs with custom spacing\n",
		count,
	)
}
```

#### Output

```
Created 4 paragraphs with custom spacing
```

</p>
</details>

<details><summary>Example (Run Formatting)</summary>
<p>

Example\_runFormatting demonstrates run\-level formatting.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
	"github.com/connerohnesorge/goffice/wordprocessing/elements"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	// Create a paragraph and add formatted runs
	pb := builder.AddParagraph("")

	rb := pb.AddRun("This is bold").Bold()
	rb.AddText(" and more bold text.")

	pb.AddRun(" This is italic.").Italic()

	pb.AddRun(" Double underline.").
		UnderlineStyle(elements.UnderlineValuesDouble)

	pb.AddRun(" Tab character:").AddTab()
	pb.AddRun("After tab.")

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	for p := range body.Paragraphs() {
		runCount := 0
		for range p.Runs() {
			runCount++
		}
		fmt.Printf(
			"Paragraph has %d runs\n",
			runCount,
		)
	}
}
```

#### Output

```
Paragraph has 5 runs
```

</p>
</details>

<details><summary>Example (Table With Formatting)</summary>
<p>

Example\_tableWithFormatting demonstrates creating formatted tables.

```go
package main

import (
	"fmt"

	"github.com/connerohnesorge/goffice/wordprocessing"
	"github.com/connerohnesorge/goffice/wordprocessing/elements"
)

func main() {
	builder := wordprocessing.NewDocumentBuilder()

	// Create a table with formatting
	tb := builder.AddTable(3, 3).
		SetWidthPercent(100).
		SetBorders(elements.BorderSingle, 4, "000000")

	// Format header row
	row := tb.Row(0)
	if row != nil {
		row.SetHeaderRow(true).
			SetHeight(400, elements.HeightRuleExact)
	}

	// Set header cells with shading
	for col := range 3 {
		cell := tb.Cell(0, col)
		if cell != nil {
			cell.SetText(fmt.Sprintf("Header %d", col+1)).
				SetShading("CCCCCC")
		}
	}

	// Fill data cells
	for row := 1; row < 3; row++ {
		for col := range 3 {
			cell := tb.Cell(row, col)
			if cell != nil {
				cell.SetText(
					fmt.Sprintf(
						"Data %d-%d",
						row,
						col+1,
					),
				)
			}
		}
	}

	doc, err := builder.Build()
	if err != nil {
		fmt.Println("Error:", err)

		return
	}

	body := doc.Body()
	for table := range body.Tables() {
		cell := table.GetCell(0, 0)
		if cell != nil {
			fmt.Println(
				"First header cell:",
				cell.InnerText(),
			)
		}
		cell = table.GetCell(1, 1)
		if cell != nil {
			fmt.Println(
				"Data cell (1,1):",
				cell.InnerText(),
			)
		}
	}
}
```

#### Output

```
First header cell: Header 1
Data cell (1,1): Data 1-2
```

</p>
</details>

## Index

- [Constants](<#constants>)
- [Variables](<#variables>)
- [func removeCommentMarkers\(parent openxml.Element, commentID int\)](<#removeCommentMarkers>)
- [func removeCommentReferenceFromRun\(run \*elements.Run, commentID int\)](<#removeCommentReferenceFromRun>)
- [type DocType](<#DocType>)
  - [func \(dt DocType\) ContentType\(\) string](<#DocType.ContentType>)
  - [func \(dt DocType\) Extension\(\) string](<#DocType.Extension>)
  - [func \(dt DocType\) String\(\) string](<#DocType.String>)
- [type Document](<#Document>)
  - [func New\(path string, docType DocType\) \(\*Document, error\)](<#New>)
  - [func NewFromTemplate\(templatePath string, attachTemplate bool\) \(\*Document, error\)](<#NewFromTemplate>)
  - [func NewWriter\(w io.Writer, docType DocType\) \(\*Document, error\)](<#NewWriter>)
  - [func Open\(path string, editable bool\) \(\*Document, error\)](<#Open>)
  - [func OpenReader\(r io.ReaderAt, size int64, editable bool\) \(\*Document, error\)](<#OpenReader>)
  - [func OpenWithSettings\(path string, editable bool, settings \*OpenSettings\) \(\*Document, error\)](<#OpenWithSettings>)
  - [func \(d \*Document\) AcceptAllRevisions\(\) error](<#Document.AcceptAllRevisions>)
  - [func \(d \*Document\) AcceptRevisionsByAuthor\(author string\) error](<#Document.AcceptRevisionsByAuthor>)
  - [func \(d \*Document\) AddComment\(author, text string\) \(\*elements.Comment, error\)](<#Document.AddComment>)
  - [func \(d \*Document\) AddFooter\(hfType elements.HeaderFooterValues\) \(\*elements.Footer, error\)](<#Document.AddFooter>)
  - [func \(d \*Document\) AddHeader\(hfType elements.HeaderFooterValues\) \(\*elements.Header, error\)](<#Document.AddHeader>)
  - [func \(d \*Document\) AddMainPart\(\) \(\*parts.MainPart, error\)](<#Document.AddMainPart>)
  - [func \(d \*Document\) ApplyComment\(para \*elements.Paragraph, startIdx, endIdx int, author, text string\) \(\*elements.Comment, error\)](<#Document.ApplyComment>)
  - [func \(d \*Document\) ChangeType\(newType DocType\) error](<#Document.ChangeType>)
  - [func \(d \*Document\) Close\(\) error](<#Document.Close>)
  - [func \(d \*Document\) CommentsPart\(\) \(\*parts.CommentsPart, error\)](<#Document.CommentsPart>)
  - [func \(d \*Document\) CoreProperties\(\) \*packaging.CoreProperties](<#Document.CoreProperties>)
  - [func \(d \*Document\) EndnotesPart\(\) \(\*parts.EndnotesPart, error\)](<#Document.EndnotesPart>)
  - [func \(d \*Document\) ExtendedProperties\(\) \*packaging.ExtendedProperties](<#Document.ExtendedProperties>)
  - [func \(d \*Document\) Features\(\) \*features.FeatureCollection](<#Document.Features>)
  - [func \(d \*Document\) Footers\(\) iter.Seq\[\*elements.Footer\]](<#Document.Footers>)
  - [func \(d \*Document\) FootnotesPart\(\) \(\*parts.FootnotesPart, error\)](<#Document.FootnotesPart>)
  - [func \(d \*Document\) GetFormFieldByName\(name string\) \*FormField](<#Document.GetFormFieldByName>)
  - [func \(d \*Document\) GetFormFields\(\) \[\]\*FormField](<#Document.GetFormFields>)
  - [func \(d \*Document\) GetOrCreateCommentsPart\(\) \(\*parts.CommentsPart, error\)](<#Document.GetOrCreateCommentsPart>)
  - [func \(d \*Document\) GetRevisions\(\) \[\]\*Revision](<#Document.GetRevisions>)
  - [func \(d \*Document\) Headers\(\) iter.Seq\[\*elements.Header\]](<#Document.Headers>)
  - [func \(d \*Document\) IsEditable\(\) bool](<#Document.IsEditable>)
  - [func \(d \*Document\) IsValid\(version validation.FileFormatVersions\) bool](<#Document.IsValid>)
  - [func \(d \*Document\) MainPart\(\) \*parts.MainPart](<#Document.MainPart>)
  - [func \(d \*Document\) Package\(\) \*openxml.OpenXmlPackage](<#Document.Package>)
  - [func \(d \*Document\) Path\(\) string](<#Document.Path>)
  - [func \(d \*Document\) RejectAllRevisions\(\) error](<#Document.RejectAllRevisions>)
  - [func \(d \*Document\) RejectRevisionsByAuthor\(author string\) error](<#Document.RejectRevisionsByAuthor>)
  - [func \(d \*Document\) RemoveComment\(commentID int\) error](<#Document.RemoveComment>)
  - [func \(d \*Document\) Save\(\) error](<#Document.Save>)
  - [func \(d \*Document\) SaveAs\(path string\) error](<#Document.SaveAs>)
  - [func \(d \*Document\) SaveTo\(w io.Writer\) error](<#Document.SaveTo>)
  - [func \(d \*Document\) Settings\(\) \*OpenSettings](<#Document.Settings>)
  - [func \(d \*Document\) Type\(\) DocType](<#Document.Type>)
  - [func \(d \*Document\) Validate\(version validation.FileFormatVersions\) validation.ValidationErrors](<#Document.Validate>)
  - [func \(d \*Document\) ValidateWithSettings\(version validation.FileFormatVersions, settings \*validation.ValidationSettings\) validation.ValidationErrors](<#Document.ValidateWithSettings>)
  - [func \(d \*Document\) attachTemplateLink\(templatePath string\) error](<#Document.attachTemplateLink>)
  - [func \(d \*Document\) detectDocumentType\(\)](<#Document.detectDocumentType>)
  - [func \(d \*Document\) initializeDocument\(\) error](<#Document.initializeDocument>)
- [type DocumentBuilder](<#DocumentBuilder>)
  - [func NewDocumentBuilder\(\) \*DocumentBuilder](<#NewDocumentBuilder>)
  - [func \(db \*DocumentBuilder\) AddColumnBreak\(\) \*DocumentBuilder](<#DocumentBuilder.AddColumnBreak>)
  - [func \(db \*DocumentBuilder\) AddHeading\(text string, level int\) \*ParagraphBuilder](<#DocumentBuilder.AddHeading>)
  - [func \(db \*DocumentBuilder\) AddHorizontalRule\(\) \*DocumentBuilder](<#DocumentBuilder.AddHorizontalRule>)
  - [func \(db \*DocumentBuilder\) AddImage\(data \[\]byte, contentType string\) \*ImageBuilder](<#DocumentBuilder.AddImage>)
  - [func \(db \*DocumentBuilder\) AddPageBreak\(\) \*DocumentBuilder](<#DocumentBuilder.AddPageBreak>)
  - [func \(db \*DocumentBuilder\) AddParagraph\(text string\) \*ParagraphBuilder](<#DocumentBuilder.AddParagraph>)
  - [func \(db \*DocumentBuilder\) AddTable\(rows, cols int\) \*TableBuilder](<#DocumentBuilder.AddTable>)
  - [func \(db \*DocumentBuilder\) Build\(\) \(\*elements.Document, error\)](<#DocumentBuilder.Build>)
  - [func \(db \*DocumentBuilder\) BuildToBytes\(\) \(\[\]byte, error\)](<#DocumentBuilder.BuildToBytes>)
  - [func \(db \*DocumentBuilder\) SetAuthor\(\_ string\) \*DocumentBuilder](<#DocumentBuilder.SetAuthor>)
  - [func \(db \*DocumentBuilder\) SetTitle\(\_ string\) \*DocumentBuilder](<#DocumentBuilder.SetTitle>)
- [type DocumentComparator](<#DocumentComparator>)
  - [func NewDocumentComparator\(\) \*DocumentComparator](<#NewDocumentComparator>)
  - [func \(dc \*DocumentComparator\) Compare\(doc1, doc2 \*Document\) \(\[\]compare.Diff, error\)](<#DocumentComparator.Compare>)
- [type DocumentMerger](<#DocumentMerger>)
  - [func NewDocumentMerger\(\) \*DocumentMerger](<#NewDocumentMerger>)
  - [func \(dm \*DocumentMerger\) Merge\(base, other \*Document\) error](<#DocumentMerger.Merge>)
  - [func \(dm \*DocumentMerger\) SetOptions\(options compare.MergeOptions\)](<#DocumentMerger.SetOptions>)
  - [func \(dm \*DocumentMerger\) SetStrategy\(strategy compare.MergeStrategy\)](<#DocumentMerger.SetStrategy>)
  - [func \(dm \*DocumentMerger\) ThreeWayMerge\(base, ours, theirs \*Document\) \(\[\]compare.Conflict, error\)](<#DocumentMerger.ThreeWayMerge>)
- [type FileFormatVersion](<#FileFormatVersion>)
- [type FormField](<#FormField>)
  - [func InsertCheckBox\(p \*elements.Paragraph, name string, checked bool\) \*FormField](<#InsertCheckBox>)
  - [func InsertDropDown\(p \*elements.Paragraph, name string, items \[\]string, selectedIndex int\) \*FormField](<#InsertDropDown>)
  - [func InsertTextField\(p \*elements.Paragraph, name, defaultValue string\) \*FormField](<#InsertTextField>)
  - [func newFormField\(simpleField \*elements.SimpleField, ffData \*elements.FormFieldData, paragraph \*elements.Paragraph, fieldType FormFieldType\) \*FormField](<#newFormField>)
  - [func \(f \*FormField\) Enabled\(\) bool](<#FormField.Enabled>)
  - [func \(f \*FormField\) GetCheckBoxSize\(\) int](<#FormField.GetCheckBoxSize>)
  - [func \(f \*FormField\) GetDefaultTextValue\(\) string](<#FormField.GetDefaultTextValue>)
  - [func \(f \*FormField\) GetDropDownItems\(\) \[\]string](<#FormField.GetDropDownItems>)
  - [func \(f \*FormField\) GetMaxLength\(\) int](<#FormField.GetMaxLength>)
  - [func \(f \*FormField\) GetSelectedIndex\(\) int](<#FormField.GetSelectedIndex>)
  - [func \(f \*FormField\) GetSelectedValue\(\) string](<#FormField.GetSelectedValue>)
  - [func \(f \*FormField\) GetTextValue\(\) string](<#FormField.GetTextValue>)
  - [func \(f \*FormField\) IsChecked\(\) bool](<#FormField.IsChecked>)
  - [func \(f \*FormField\) Name\(\) string](<#FormField.Name>)
  - [func \(f \*FormField\) Remove\(\)](<#FormField.Remove>)
  - [func \(f \*FormField\) SetCheckBoxSize\(size int\)](<#FormField.SetCheckBoxSize>)
  - [func \(f \*FormField\) SetChecked\(isChecked bool\)](<#FormField.SetChecked>)
  - [func \(f \*FormField\) SetDefaultTextValue\(value string\)](<#FormField.SetDefaultTextValue>)
  - [func \(f \*FormField\) SetDropDownItems\(items \[\]string\)](<#FormField.SetDropDownItems>)
  - [func \(f \*FormField\) SetEnabled\(isEnabled bool\)](<#FormField.SetEnabled>)
  - [func \(f \*FormField\) SetMaxLength\(length int\)](<#FormField.SetMaxLength>)
  - [func \(f \*FormField\) SetName\(name string\)](<#FormField.SetName>)
  - [func \(f \*FormField\) SetSelectedIndex\(index int\)](<#FormField.SetSelectedIndex>)
  - [func \(f \*FormField\) SetTextValue\(value string\)](<#FormField.SetTextValue>)
  - [func \(f \*FormField\) Type\(\) FormFieldType](<#FormField.Type>)
- [type FormFieldType](<#FormFieldType>)
  - [func detectFormFieldType\(instruction string\) \(FormFieldType, error\)](<#detectFormFieldType>)
  - [func \(t FormFieldType\) String\(\) string](<#FormFieldType.String>)
- [type ImageBuilder](<#ImageBuilder>)
  - [func \(ib \*ImageBuilder\) Document\(\) \*DocumentBuilder](<#ImageBuilder.Document>)
  - [func \(ib \*ImageBuilder\) Height\(emus int64\) \*ImageBuilder](<#ImageBuilder.Height>)
  - [func \(ib \*ImageBuilder\) HeightCm\(cm float64\) \*ImageBuilder](<#ImageBuilder.HeightCm>)
  - [func \(ib \*ImageBuilder\) HeightInches\(inches float64\) \*ImageBuilder](<#ImageBuilder.HeightInches>)
  - [func \(ib \*ImageBuilder\) Inline\(\) \*ImageBuilder](<#ImageBuilder.Inline>)
  - [func \(ib \*ImageBuilder\) Insert\(\) \*DocumentBuilder](<#ImageBuilder.Insert>)
  - [func \(ib \*ImageBuilder\) Size\(width, height int64\) \*ImageBuilder](<#ImageBuilder.Size>)
  - [func \(ib \*ImageBuilder\) SizeCm\(width, height float64\) \*ImageBuilder](<#ImageBuilder.SizeCm>)
  - [func \(ib \*ImageBuilder\) SizeInches\(width, height float64\) \*ImageBuilder](<#ImageBuilder.SizeInches>)
  - [func \(ib \*ImageBuilder\) Width\(emus int64\) \*ImageBuilder](<#ImageBuilder.Width>)
  - [func \(ib \*ImageBuilder\) WidthCm\(cm float64\) \*ImageBuilder](<#ImageBuilder.WidthCm>)
  - [func \(ib \*ImageBuilder\) WidthInches\(inches float64\) \*ImageBuilder](<#ImageBuilder.WidthInches>)
- [type MarkupCompatibilityProcessSettings](<#MarkupCompatibilityProcessSettings>)
- [type OpenSettings](<#OpenSettings>)
  - [func DefaultOpenSettings\(\) \*OpenSettings](<#DefaultOpenSettings>)
  - [func \(s \*OpenSettings\) WithAutoSave\(autoSave bool\) \*OpenSettings](<#OpenSettings.WithAutoSave>)
  - [func \(s \*OpenSettings\) WithMaxCharacters\(maxChars int64\) \*OpenSettings](<#OpenSettings.WithMaxCharacters>)
  - [func \(s \*OpenSettings\) WithTargetVersion\(version FileFormatVersion\) \*OpenSettings](<#OpenSettings.WithTargetVersion>)
- [type ParagraphBuilder](<#ParagraphBuilder>)
  - [func \(pb \*ParagraphBuilder\) AddLineBreak\(\) \*ParagraphBuilder](<#ParagraphBuilder.AddLineBreak>)
  - [func \(pb \*ParagraphBuilder\) AddRun\(text string\) \*RunBuilder](<#ParagraphBuilder.AddRun>)
  - [func \(pb \*ParagraphBuilder\) Align\(alignment elements.JustificationValue\) \*ParagraphBuilder](<#ParagraphBuilder.Align>)
  - [func \(pb \*ParagraphBuilder\) AlignCenter\(\) \*ParagraphBuilder](<#ParagraphBuilder.AlignCenter>)
  - [func \(pb \*ParagraphBuilder\) AlignJustify\(\) \*ParagraphBuilder](<#ParagraphBuilder.AlignJustify>)
  - [func \(pb \*ParagraphBuilder\) AlignLeft\(\) \*ParagraphBuilder](<#ParagraphBuilder.AlignLeft>)
  - [func \(pb \*ParagraphBuilder\) AlignRight\(\) \*ParagraphBuilder](<#ParagraphBuilder.AlignRight>)
  - [func \(pb \*ParagraphBuilder\) Bold\(\) \*ParagraphBuilder](<#ParagraphBuilder.Bold>)
  - [func \(pb \*ParagraphBuilder\) Color\(hex string\) \*ParagraphBuilder](<#ParagraphBuilder.Color>)
  - [func \(pb \*ParagraphBuilder\) Document\(\) \*DocumentBuilder](<#ParagraphBuilder.Document>)
  - [func \(pb \*ParagraphBuilder\) FirstLineIndent\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.FirstLineIndent>)
  - [func \(pb \*ParagraphBuilder\) Font\(name string, size int\) \*ParagraphBuilder](<#ParagraphBuilder.Font>)
  - [func \(pb \*ParagraphBuilder\) FontSize\(size int\) \*ParagraphBuilder](<#ParagraphBuilder.FontSize>)
  - [func \(pb \*ParagraphBuilder\) HangingIndent\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.HangingIndent>)
  - [func \(pb \*ParagraphBuilder\) Highlight\(color elements.HighlightColor\) \*ParagraphBuilder](<#ParagraphBuilder.Highlight>)
  - [func \(pb \*ParagraphBuilder\) Italic\(\) \*ParagraphBuilder](<#ParagraphBuilder.Italic>)
  - [func \(pb \*ParagraphBuilder\) LeftIndent\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.LeftIndent>)
  - [func \(pb \*ParagraphBuilder\) Paragraph\(\) \*elements.Paragraph](<#ParagraphBuilder.Paragraph>)
  - [func \(pb \*ParagraphBuilder\) RightIndent\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.RightIndent>)
  - [func \(pb \*ParagraphBuilder\) SpacingAfter\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.SpacingAfter>)
  - [func \(pb \*ParagraphBuilder\) SpacingBefore\(points int\) \*ParagraphBuilder](<#ParagraphBuilder.SpacingBefore>)
  - [func \(pb \*ParagraphBuilder\) Style\(styleId string\) \*ParagraphBuilder](<#ParagraphBuilder.Style>)
  - [func \(pb \*ParagraphBuilder\) Underline\(\) \*ParagraphBuilder](<#ParagraphBuilder.Underline>)
- [type ProcessMode](<#ProcessMode>)
- [type Revision](<#Revision>)
  - [func collectRevisionsFromElement\(elem openxml.Element, doc \*Document\) \[\]\*Revision](<#collectRevisionsFromElement>)
  - [func newRevision\(element any, parent openxml.Element, doc \*Document\) \*Revision](<#newRevision>)
  - [func \(r \*Revision\) Accept\(\) error](<#Revision.Accept>)
  - [func \(r \*Revision\) Author\(\) string](<#Revision.Author>)
  - [func \(r \*Revision\) Content\(\) string](<#Revision.Content>)
  - [func \(r \*Revision\) Date\(\) time.Time](<#Revision.Date>)
  - [func \(r \*Revision\) Document\(\) \*Document](<#Revision.Document>)
  - [func \(r \*Revision\) Element\(\) any](<#Revision.Element>)
  - [func \(r \*Revision\) Id\(\) int](<#Revision.Id>)
  - [func \(r \*Revision\) Parent\(\) openxml.Element](<#Revision.Parent>)
  - [func \(r \*Revision\) Reject\(\) error](<#Revision.Reject>)
  - [func \(r \*Revision\) Type\(\) RevisionType](<#Revision.Type>)
  - [func \(r \*Revision\) acceptDeletedRun\(\) error](<#Revision.acceptDeletedRun>)
  - [func \(r \*Revision\) acceptFormatChange\(\) error](<#Revision.acceptFormatChange>)
  - [func \(r \*Revision\) acceptInsertedRun\(\) error](<#Revision.acceptInsertedRun>)
  - [func \(\*Revision\) acceptMoveRun\(\) error](<#Revision.acceptMoveRun>)
  - [func \(\*Revision\) convertDeletedTextsInRun\(run \*elements.Run\)](<#Revision.convertDeletedTextsInRun>)
  - [func \(r \*Revision\) rejectDeletedRun\(\) error](<#Revision.rejectDeletedRun>)
  - [func \(r \*Revision\) rejectFormatChange\(\) error](<#Revision.rejectFormatChange>)
  - [func \(r \*Revision\) rejectInsertedRun\(\) error](<#Revision.rejectInsertedRun>)
  - [func \(\*Revision\) rejectMoveRun\(\) error](<#Revision.rejectMoveRun>)
  - [func \(\*Revision\) rejectParagraphPropertiesChange\(parent openxml.CompositeElement, change \*elements.ParagraphPropertiesChange\)](<#Revision.rejectParagraphPropertiesChange>)
  - [func \(\*Revision\) rejectRunPropertiesChange\(parent openxml.CompositeElement, change \*elements.RunPropertiesChange\)](<#Revision.rejectRunPropertiesChange>)
- [type RevisionCollection](<#RevisionCollection>)
  - [func NewRevisionCollection\(revisions \[\]\*Revision\) \*RevisionCollection](<#NewRevisionCollection>)
  - [func \(rc \*RevisionCollection\) All\(\) \[\]\*Revision](<#RevisionCollection.All>)
  - [func \(rc \*RevisionCollection\) ByAuthor\(author string\) \[\]\*Revision](<#RevisionCollection.ByAuthor>)
  - [func \(rc \*RevisionCollection\) ByType\(typ RevisionType\) \[\]\*Revision](<#RevisionCollection.ByType>)
  - [func \(rc \*RevisionCollection\) Count\(\) int](<#RevisionCollection.Count>)
- [type RevisionType](<#RevisionType>)
  - [func \(rt RevisionType\) String\(\) string](<#RevisionType.String>)
- [type RunBuilder](<#RunBuilder>)
  - [func \(rb \*RunBuilder\) AddBreak\(breakType elements.BreakType\) \*RunBuilder](<#RunBuilder.AddBreak>)
  - [func \(rb \*RunBuilder\) AddTab\(\) \*RunBuilder](<#RunBuilder.AddTab>)
  - [func \(rb \*RunBuilder\) AddText\(text string\) \*RunBuilder](<#RunBuilder.AddText>)
  - [func \(rb \*RunBuilder\) Bold\(\) \*RunBuilder](<#RunBuilder.Bold>)
  - [func \(rb \*RunBuilder\) Color\(hex string\) \*RunBuilder](<#RunBuilder.Color>)
  - [func \(rb \*RunBuilder\) Document\(\) \*DocumentBuilder](<#RunBuilder.Document>)
  - [func \(rb \*RunBuilder\) Font\(name string\) \*RunBuilder](<#RunBuilder.Font>)
  - [func \(rb \*RunBuilder\) FontSize\(points int\) \*RunBuilder](<#RunBuilder.FontSize>)
  - [func \(rb \*RunBuilder\) Highlight\(color elements.HighlightColor\) \*RunBuilder](<#RunBuilder.Highlight>)
  - [func \(rb \*RunBuilder\) Italic\(\) \*RunBuilder](<#RunBuilder.Italic>)
  - [func \(rb \*RunBuilder\) Paragraph\(\) \*ParagraphBuilder](<#RunBuilder.Paragraph>)
  - [func \(rb \*RunBuilder\) Run\(\) \*elements.Run](<#RunBuilder.Run>)
  - [func \(rb \*RunBuilder\) Underline\(\) \*RunBuilder](<#RunBuilder.Underline>)
  - [func \(rb \*RunBuilder\) UnderlineStyle\(style elements.UnderlineValue\) \*RunBuilder](<#RunBuilder.UnderlineStyle>)
- [type TableBuilder](<#TableBuilder>)
  - [func \(tb \*TableBuilder\) AddRow\(\) \*TableBuilder](<#TableBuilder.AddRow>)
  - [func \(tb \*TableBuilder\) Cell\(row, col int\) \*TableCellBuilder](<#TableBuilder.Cell>)
  - [func \(tb \*TableBuilder\) Document\(\) \*DocumentBuilder](<#TableBuilder.Document>)
  - [func \(tb \*TableBuilder\) Row\(index int\) \*TableRowBuilder](<#TableBuilder.Row>)
  - [func \(tb \*TableBuilder\) SetBorders\(style elements.BorderStyle, size int, color string\) \*TableBuilder](<#TableBuilder.SetBorders>)
  - [func \(tb \*TableBuilder\) SetCellText\(row, col int, text string\) \*TableBuilder](<#TableBuilder.SetCellText>)
  - [func \(tb \*TableBuilder\) SetColumnWidth\(col int, width int\) \*TableBuilder](<#TableBuilder.SetColumnWidth>)
  - [func \(tb \*TableBuilder\) SetColumnWidthInches\(col int, inches float64\) \*TableBuilder](<#TableBuilder.SetColumnWidthInches>)
  - [func \(tb \*TableBuilder\) SetStyle\(styleId string\) \*TableBuilder](<#TableBuilder.SetStyle>)
  - [func \(tb \*TableBuilder\) SetWidth\(width int, widthType elements.TableWidthType\) \*TableBuilder](<#TableBuilder.SetWidth>)
  - [func \(tb \*TableBuilder\) SetWidthPercent\(percent int\) \*TableBuilder](<#TableBuilder.SetWidthPercent>)
  - [func \(tb \*TableBuilder\) Table\(\) \*elements.Table](<#TableBuilder.Table>)
- [type TableCellBuilder](<#TableCellBuilder>)
  - [func \(tcb \*TableCellBuilder\) AddParagraph\(text string\) \*ParagraphBuilder](<#TableCellBuilder.AddParagraph>)
  - [func \(tcb \*TableCellBuilder\) Cell\(\) \*elements.TableCell](<#TableCellBuilder.Cell>)
  - [func \(tcb \*TableCellBuilder\) SetBorders\(style elements.BorderStyle, size int, color string\) \*TableCellBuilder](<#TableCellBuilder.SetBorders>)
  - [func \(tcb \*TableCellBuilder\) SetHorizontalMerge\(span int\) \*TableCellBuilder](<#TableCellBuilder.SetHorizontalMerge>)
  - [func \(tcb \*TableCellBuilder\) SetShading\(fillColor string\) \*TableCellBuilder](<#TableCellBuilder.SetShading>)
  - [func \(tcb \*TableCellBuilder\) SetText\(text string\) \*TableCellBuilder](<#TableCellBuilder.SetText>)
  - [func \(tcb \*TableCellBuilder\) SetVerticalMerge\(mergeType elements.VerticalMergeType\) \*TableCellBuilder](<#TableCellBuilder.SetVerticalMerge>)
  - [func \(tcb \*TableCellBuilder\) SetWidth\(width int, widthType elements.TableWidthType\) \*TableCellBuilder](<#TableCellBuilder.SetWidth>)
  - [func \(tcb \*TableCellBuilder\) Table\(\) \*TableBuilder](<#TableCellBuilder.Table>)
- [type TableRowBuilder](<#TableRowBuilder>)
  - [func \(t \*TableRowBuilder\) Cell\(col int\) \*TableCellBuilder](<#TableRowBuilder.Cell>)
  - [func \(t \*TableRowBuilder\) Row\(\) \*elements.TableRow](<#TableRowBuilder.Row>)
  - [func \(t \*TableRowBuilder\) SetHeaderRow\(isHeader bool\) \*TableRowBuilder](<#TableRowBuilder.SetHeaderRow>)
  - [func \(t \*TableRowBuilder\) SetHeight\(height int, rule elements.HeightRule\) \*TableRowBuilder](<#TableRowBuilder.SetHeight>)
  - [func \(t \*TableRowBuilder\) Table\(\) \*TableBuilder](<#TableRowBuilder.Table>)
- [type docError](<#docError>)
  - [func \(e docError\) Error\(\) string](<#docError.Error>)
- [type imageData](<#imageData>)


## Constants

<a name="maxHeadingLevel"></a>

```go
const (
    maxHeadingLevel    = 9
    emuPerInch         = 914400 // EMUs per inch
    defaultBorderWidth = 6      // default border width in eighths of a point
    twipsPerPoint      = 20     // twips per point
    pctMultiplier      = 50     // multiplier for percentage calculations
)
```

<a name="extensionDocx"></a>File extension constants

```go
const (
    extensionDocx = ".docx"
    extensionDotx = ".dotx"
    extensionDocm = ".docm"
    extensionDotm = ".dotm"
)
```

<a name="ContentTypeWordMLDocument"></a>Content types for Word documents.

```go
const (
    // ContentTypeWordMLDocument is the content type for a standard
    // Word document main part.
    ContentTypeWordMLDocument = "application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml"

    // ContentTypeWordMLTemplate is the content type for a Word
    // template main part.
    ContentTypeWordMLTemplate = "application/vnd.openxmlformats-officedocument.wordprocessingml.template.main+xml"

    // ContentTypeWordMLMacroEnabled is the content type for a
    // macro-enabled Word document main part.
    ContentTypeWordMLMacroEnabled = "application/vnd.ms-word.document.macroEnabled.main+xml"

    // ContentTypeWordMLMacroTemplate is the content type for a
    // macro-enabled Word template main part.
    ContentTypeWordMLMacroTemplate = "application/vnd.ms-word.template.macroEnabled.main+xml"
)
```

<a name="maxDropDownItems"></a>

```go
const (
    // maxDropDownItems is the maximum number of items allowed in a dropdown per Word specification.
    maxDropDownItems = 25
    // intFormatSpecifier is the format specifier for integer conversion.
    intFormatSpecifier = "%d"
    // fldSimpleLocalName is the local name for SimpleField elements.
    fldSimpleLocalName = "fldSimple"
    // ffDataLocalName is the local name for FormFieldData elements.
    ffDataLocalName = "ffData"
)
```

## Variables

<a name="ErrReadOnly"></a>Errors

```go
var (
    ErrReadOnly = docError(
        "document is read-only",
    )
    ErrNoPath = docError(
        "document has no file path",
    )
    ErrPackageNil     = docError("package is nil")
    ErrMainPartExists = docError(
        "main document part already exists",
    )
    ErrNoMainPart = docError(
        "no main document part",
    )
)
```

<a name="ErrMoveCoordinationNotImplemented"></a>ErrMoveCoordinationNotImplemented is returned when attempting to accept/reject move operations, which require coordinated handling of move pairs.

```go
var ErrMoveCoordinationNotImplemented = errors.New(
    "move pair coordination not yet implemented",
)
```

<a name="_"></a>Ensure Document implements io.Closer.

```go
var _ io.Closer = (*Document)(nil)
```

<a name="removeCommentMarkers"></a>
## func removeCommentMarkers

```go
func removeCommentMarkers(parent openxml.Element, commentID int)
```

removeCommentMarkers recursively removes all comment markers \(CommentRangeStart, CommentRangeEnd, and CommentReference\) with the specified comment ID from the given element and its descendants.

<a name="removeCommentReferenceFromRun"></a>
## func removeCommentReferenceFromRun

```go
func removeCommentReferenceFromRun(run *elements.Run, commentID int)
```

removeCommentReferenceFromRun removes CommentReference elements with the specified comment ID from a Run.

<a name="DocType"></a>
## type DocType

DocType represents the type of a Word document.

```go
type DocType int
```

<a name="DocTypeDocument"></a>

```go
const (
    // DocTypeDocument represents a standard Word document (.docx).
    DocTypeDocument DocType = iota

    // DocTypeTemplate represents a Word template (.dotx).
    DocTypeTemplate

    // DocTypeMacroEnabled represents a macro-enabled Word document (.docm).
    DocTypeMacroEnabled

    // DocTypeMacroTemplate represents a macro-enabled Word template (.dotm).
    DocTypeMacroTemplate
)
```

<a name="DocType.ContentType"></a>
### func \(DocType\) ContentType

```go
func (dt DocType) ContentType() string
```

ContentType returns the MIME content type for the main document part.

<a name="DocType.Extension"></a>
### func \(DocType\) Extension

```go
func (dt DocType) Extension() string
```

Extension returns the file extension for this document type.

<a name="DocType.String"></a>
### func \(DocType\) String

```go
func (dt DocType) String() string
```

String returns the string representation of the document type.

<a name="Document"></a>
## type Document

Document represents a Word document \(.docx, .dotx, .docm, .dotm\). It wraps an OpenXmlPackage and provides Word\-specific functionality.

```go
type Document struct {
    // pkg is the underlying OpenXML package.
    pkg *openxml.OpenXmlPackage

    // docType is the type of this document.
    docType DocType

    // mainPart is the cached main document part.
    mainPart *parts.MainPart

    // settings are the options used to open/create this document.
    settings *OpenSettings

    // path is the file path (if opened from file).
    path string

    // isEditable indicates if the document was opened for editing.
    isEditable bool
}
```

<a name="New"></a>
### func New

```go
func New(path string, docType DocType) (*Document, error)
```

New creates a new Word document at the specified path. The document type determines the file extension and content types.

<a name="NewFromTemplate"></a>
### func NewFromTemplate

```go
func NewFromTemplate(templatePath string, attachTemplate bool) (*Document, error)
```

NewFromTemplate creates a new document from a template. If attachTemplate is true, the template remains linked to the document.

<a name="NewWriter"></a>
### func NewWriter

```go
func NewWriter(w io.Writer, docType DocType) (*Document, error)
```

NewWriter creates a new Word document that writes to the given io.Writer.

<a name="Open"></a>
### func Open

```go
func Open(path string, editable bool) (*Document, error)
```

Open opens an existing Word document from the specified path. If editable is true, the document can be modified and saved.

<a name="OpenReader"></a>
### func OpenReader

```go
func OpenReader(r io.ReaderAt, size int64, editable bool) (*Document, error)
```

OpenReader opens a Word document from an io.ReaderAt.

<a name="OpenWithSettings"></a>
### func OpenWithSettings

```go
func OpenWithSettings(path string, editable bool, settings *OpenSettings) (*Document, error)
```

OpenWithSettings opens a Word document with custom settings.

<a name="Document.AcceptAllRevisions"></a>
### func \(\*Document\) AcceptAllRevisions

```go
func (d *Document) AcceptAllRevisions() error
```

AcceptAllRevisions accepts all revisions in the document. This applies all tracked changes and removes the revision markup.

<a name="Document.AcceptRevisionsByAuthor"></a>
### func \(\*Document\) AcceptRevisionsByAuthor

```go
func (d *Document) AcceptRevisionsByAuthor(author string) error
```

AcceptRevisionsByAuthor accepts all revisions made by the specified author.

<a name="Document.AddComment"></a>
### func \(\*Document\) AddComment

```go
func (d *Document) AddComment(author, text string) (*elements.Comment, error)
```

AddComment adds a new comment to the document and returns it.

<a name="Document.AddFooter"></a>
### func \(\*Document\) AddFooter

```go
func (d *Document) AddFooter(hfType elements.HeaderFooterValues) (*elements.Footer, error)
```

AddFooter adds a new footer part of the specified type and returns the Footer element. The footer is also linked to the document's section properties.

<a name="Document.AddHeader"></a>
### func \(\*Document\) AddHeader

```go
func (d *Document) AddHeader(hfType elements.HeaderFooterValues) (*elements.Header, error)
```

AddHeader adds a new header part of the specified type and returns the Header element. The header is also linked to the document's section properties.

<a name="Document.AddMainPart"></a>
### func \(\*Document\) AddMainPart

```go
func (d *Document) AddMainPart() (*parts.MainPart, error)
```

AddMainPart creates and adds the main document part. Returns an error if a main part already exists.

<a name="Document.ApplyComment"></a>
### func \(\*Document\) ApplyComment

```go
func (d *Document) ApplyComment(para *elements.Paragraph, startIdx, endIdx int, author, text string) (*elements.Comment, error)
```

ApplyComment creates a new comment and marks the specified range of runs in the paragraph with comment markers.

Parameters:

- para: The paragraph to apply the comment to
- startIdx: 0\-based index of the first run in the comment range
- endIdx: 0\-based index of the last run in the comment range
- author: The author of the comment
- text: The comment text content

Returns the created Comment element and an error if the operation fails. If marking the range fails, the comment is rolled back \(removed from the part\).

<a name="Document.ChangeType"></a>
### func \(\*Document\) ChangeType

```go
func (d *Document) ChangeType(newType DocType) error
```

ChangeType changes the document type. This updates the content type of the main document part.

<a name="Document.Close"></a>
### func \(\*Document\) Close

```go
func (d *Document) Close() error
```

Close closes the document and releases all resources. If AutoSave is enabled and the document has unsaved changes, it will be saved.

<a name="Document.CommentsPart"></a>
### func \(\*Document\) CommentsPart

```go
func (d *Document) CommentsPart() (*parts.CommentsPart, error)
```

CommentsPart returns the comments part, creating it if it doesn't exist.

<a name="Document.CoreProperties"></a>
### func \(\*Document\) CoreProperties

```go
func (d *Document) CoreProperties() *packaging.CoreProperties
```

CoreProperties returns the core file properties part. Returns nil if not present.

<a name="Document.EndnotesPart"></a>
### func \(\*Document\) EndnotesPart

```go
func (d *Document) EndnotesPart() (*parts.EndnotesPart, error)
```

EndnotesPart returns the endnotes part, creating it if it doesn't exist.

<a name="Document.ExtendedProperties"></a>
### func \(\*Document\) ExtendedProperties

```go
func (d *Document) ExtendedProperties() *packaging.ExtendedProperties
```

ExtendedProperties returns the extended file properties part. Returns nil if not present.

<a name="Document.Features"></a>
### func \(\*Document\) Features

```go
func (d *Document) Features() *features.FeatureCollection
```

Features returns the document's feature collection.

<a name="Document.Footers"></a>
### func \(\*Document\) Footers

```go
func (d *Document) Footers() iter.Seq[*elements.Footer]
```

Footers returns an iterator over all Footer elements in the document.

<a name="Document.FootnotesPart"></a>
### func \(\*Document\) FootnotesPart

```go
func (d *Document) FootnotesPart() (*parts.FootnotesPart, error)
```

FootnotesPart returns the footnotes part, creating it if it doesn't exist.

<a name="Document.GetFormFieldByName"></a>
### func \(\*Document\) GetFormFieldByName

```go
func (d *Document) GetFormFieldByName(name string) *FormField
```

GetFormFieldByName finds and returns a form field by its name. Returns nil if no form field with the given name is found.

<a name="Document.GetFormFields"></a>
### func \(\*Document\) GetFormFields

```go
func (d *Document) GetFormFields() []*FormField
```

GetFormFields returns all form fields in the document. It traverses the document body and collects all SimpleField elements that contain FormFieldData.

<a name="Document.GetOrCreateCommentsPart"></a>
### func \(\*Document\) GetOrCreateCommentsPart

```go
func (d *Document) GetOrCreateCommentsPart() (*parts.CommentsPart, error)
```

GetOrCreateCommentsPart returns the comments part, creating it if it doesn't exist. This is a convenience method that wraps CommentsPart\(\) to always return a valid part.

<a name="Document.GetRevisions"></a>
### func \(\*Document\) GetRevisions

```go
func (d *Document) GetRevisions() []*Revision
```

GetRevisions returns all revisions in the document. It traverses the entire document body, headers, and footers to collect all revision tracking elements.

<a name="Document.Headers"></a>
### func \(\*Document\) Headers

```go
func (d *Document) Headers() iter.Seq[*elements.Header]
```

Headers returns an iterator over all Header elements in the document.

<a name="Document.IsEditable"></a>
### func \(\*Document\) IsEditable

```go
func (d *Document) IsEditable() bool
```

IsEditable returns true if the document was opened for editing.

<a name="Document.IsValid"></a>
### func \(\*Document\) IsValid

```go
func (d *Document) IsValid(version validation.FileFormatVersions) bool
```

IsValid returns true if the document has no validation errors for the specified version. This is a convenience method that calls Validate and checks if there are any errors.

<a name="Document.MainPart"></a>
### func \(\*Document\) MainPart

```go
func (d *Document) MainPart() *parts.MainPart
```

MainPart returns the main document part. Returns nil if no main document part exists or the document is closed.

<a name="Document.Package"></a>
### func \(\*Document\) Package

```go
func (d *Document) Package() *openxml.OpenXmlPackage
```

Package returns the underlying OpenXML package.

<a name="Document.Path"></a>
### func \(\*Document\) Path

```go
func (d *Document) Path() string
```

Path returns the file path of the document. Returns empty string if the document was created from a stream.

<a name="Document.RejectAllRevisions"></a>
### func \(\*Document\) RejectAllRevisions

```go
func (d *Document) RejectAllRevisions() error
```

RejectAllRevisions rejects all revisions in the document. This reverts all tracked changes and removes the revision markup.

<a name="Document.RejectRevisionsByAuthor"></a>
### func \(\*Document\) RejectRevisionsByAuthor

```go
func (d *Document) RejectRevisionsByAuthor(author string) error
```

RejectRevisionsByAuthor rejects all revisions made by the specified author.

<a name="Document.RemoveComment"></a>
### func \(\*Document\) RemoveComment

```go
func (d *Document) RemoveComment(commentID int) error
```

RemoveComment removes a comment by ID from the document. This includes removing: \- The comment from the CommentsPart \- All CommentRangeStart markers with matching ID \- All CommentRangeEnd markers with matching ID \- All CommentReference elements with matching ID

Returns an error if the comment is not found.

<a name="Document.Save"></a>
### func \(\*Document\) Save

```go
func (d *Document) Save() error
```

Save saves the document to its original location. Returns an error if the document is read\-only or was created from a stream.

<a name="Document.SaveAs"></a>
### func \(\*Document\) SaveAs

```go
func (d *Document) SaveAs(path string) error
```

SaveAs saves the document to a new file path.

<a name="Document.SaveTo"></a>
### func \(\*Document\) SaveTo

```go
func (d *Document) SaveTo(w io.Writer) error
```

SaveTo writes the document to the given io.Writer. The document is written as a ZIP \(Office Open XML format\) containing all parts, relationships, and content types.

<a name="Document.Settings"></a>
### func \(\*Document\) Settings

```go
func (d *Document) Settings() *OpenSettings
```

Settings returns the open settings for this document.

<a name="Document.Type"></a>
### func \(\*Document\) Type

```go
func (d *Document) Type() DocType
```

Type returns the document type.

<a name="Document.Validate"></a>
### func \(\*Document\) Validate

```go
func (d *Document) Validate(version validation.FileFormatVersions) validation.ValidationErrors
```

Validate validates the document structure against the specified Office version. It checks for schema compliance and semantic constraints. Returns validation errors if any issues are found, or an empty slice if valid.

<a name="Document.ValidateWithSettings"></a>
### func \(\*Document\) ValidateWithSettings

```go
func (d *Document) ValidateWithSettings(version validation.FileFormatVersions, settings *validation.ValidationSettings) validation.ValidationErrors
```

ValidateWithSettings validates the document with custom validation settings. This allows control over validation behavior such as maximum errors to collect, whether to validate semantics, and strict mode.

<a name="Document.attachTemplateLink"></a>
### func \(\*Document\) attachTemplateLink

```go
func (d *Document) attachTemplateLink(templatePath string) error
```

attachTemplateLink attaches the template to the document by creating an external relationship and adding the attachedTemplate element to settings.

<a name="Document.detectDocumentType"></a>
### func \(\*Document\) detectDocumentType

```go
func (d *Document) detectDocumentType()
```

detectDocumentType determines the document type from the main part content type.

<a name="Document.initializeDocument"></a>
### func \(\*Document\) initializeDocument

```go
func (d *Document) initializeDocument() error
```

initializeDocument sets up the basic document structure for a new document.

<a name="DocumentBuilder"></a>
## type DocumentBuilder

DocumentBuilder provides a fluent API for creating Word documents.

```go
type DocumentBuilder struct {
    document *elements.Document
    images   []imageData
    errors   []error
}
```

<a name="NewDocumentBuilder"></a>
### func NewDocumentBuilder

```go
func NewDocumentBuilder() *DocumentBuilder
```

NewDocumentBuilder creates a new DocumentBuilder.

<a name="DocumentBuilder.AddColumnBreak"></a>
### func \(\*DocumentBuilder\) AddColumnBreak

```go
func (db *DocumentBuilder) AddColumnBreak() *DocumentBuilder
```

AddColumnBreak adds a column break to the document.

<a name="DocumentBuilder.AddHeading"></a>
### func \(\*DocumentBuilder\) AddHeading

```go
func (db *DocumentBuilder) AddHeading(text string, level int) *ParagraphBuilder
```

AddHeading adds a heading paragraph with the given text and level \(1\-9\).

<a name="DocumentBuilder.AddHorizontalRule"></a>
### func \(\*DocumentBuilder\) AddHorizontalRule

```go
func (db *DocumentBuilder) AddHorizontalRule() *DocumentBuilder
```

AddHorizontalRule adds a horizontal rule \(paragraph border\) to the document.

<a name="DocumentBuilder.AddImage"></a>
### func \(\*DocumentBuilder\) AddImage

```go
func (db *DocumentBuilder) AddImage(data []byte, contentType string) *ImageBuilder
```

AddImage adds an image to the document. The image will be added as an inline drawing. Width and height are in EMUs \(914400 EMU = 1 inch\).

<a name="DocumentBuilder.AddPageBreak"></a>
### func \(\*DocumentBuilder\) AddPageBreak

```go
func (db *DocumentBuilder) AddPageBreak() *DocumentBuilder
```

AddPageBreak adds a page break to the document.

<a name="DocumentBuilder.AddParagraph"></a>
### func \(\*DocumentBuilder\) AddParagraph

```go
func (db *DocumentBuilder) AddParagraph(text string) *ParagraphBuilder
```

AddParagraph adds a paragraph with the given text. It returns a ParagraphBuilder for further customization.

<a name="DocumentBuilder.AddTable"></a>
### func \(\*DocumentBuilder\) AddTable

```go
func (db *DocumentBuilder) AddTable(rows, cols int) *TableBuilder
```

AddTable adds a table with the specified number of rows and columns.

<a name="DocumentBuilder.Build"></a>
### func \(\*DocumentBuilder\) Build

```go
func (db *DocumentBuilder) Build() (*elements.Document, error)
```

Build creates the final Document from the builder.

<a name="DocumentBuilder.BuildToBytes"></a>
### func \(\*DocumentBuilder\) BuildToBytes

```go
func (db *DocumentBuilder) BuildToBytes() ([]byte, error)
```

BuildToBytes creates the document and returns it as a byte slice.

<a name="DocumentBuilder.SetAuthor"></a>
### func \(\*DocumentBuilder\) SetAuthor

```go
func (db *DocumentBuilder) SetAuthor(_ string) *DocumentBuilder
```

SetAuthor sets the document author \(for core properties\).

<a name="DocumentBuilder.SetTitle"></a>
### func \(\*DocumentBuilder\) SetTitle

```go
func (db *DocumentBuilder) SetTitle(_ string) *DocumentBuilder
```

SetTitle sets the document title \(for core properties\).

<a name="DocumentComparator"></a>
## type DocumentComparator

DocumentComparator compares two Word documents.

```go
type DocumentComparator struct {
    comparator compare.Comparator
}
```

<a name="NewDocumentComparator"></a>
### func NewDocumentComparator

```go
func NewDocumentComparator() *DocumentComparator
```

NewDocumentComparator creates a new DocumentComparator.

<a name="DocumentComparator.Compare"></a>
### func \(\*DocumentComparator\) Compare

```go
func (dc *DocumentComparator) Compare(doc1, doc2 *Document) ([]compare.Diff, error)
```

Compare compares two documents and returns the differences.

<a name="DocumentMerger"></a>
## type DocumentMerger

DocumentMerger merges Word documents.

```go
type DocumentMerger struct {
    merger compare.Merger
}
```

<a name="NewDocumentMerger"></a>
### func NewDocumentMerger

```go
func NewDocumentMerger() *DocumentMerger
```

NewDocumentMerger creates a new DocumentMerger.

<a name="DocumentMerger.Merge"></a>
### func \(\*DocumentMerger\) Merge

```go
func (dm *DocumentMerger) Merge(base, other *Document) error
```

Merge merges 'other' into 'base'. The 'base' document is modified in place.

<a name="DocumentMerger.SetOptions"></a>
### func \(\*DocumentMerger\) SetOptions

```go
func (dm *DocumentMerger) SetOptions(options compare.MergeOptions)
```

SetOptions sets the merge options.

<a name="DocumentMerger.SetStrategy"></a>
### func \(\*DocumentMerger\) SetStrategy

```go
func (dm *DocumentMerger) SetStrategy(strategy compare.MergeStrategy)
```

SetStrategy sets the merge strategy.

<a name="DocumentMerger.ThreeWayMerge"></a>
### func \(\*DocumentMerger\) ThreeWayMerge

```go
func (dm *DocumentMerger) ThreeWayMerge(base, ours, theirs *Document) ([]compare.Conflict, error)
```

ThreeWayMerge merges 'ours' and 'theirs' into 'base'. Currently, this modifies 'base' in place to reflect the merge result. It returns the list of conflicts found.

<a name="FileFormatVersion"></a>
## type FileFormatVersion

FileFormatVersion represents the target Office version.

```go
type FileFormatVersion int
```

<a name="FileFormatVersionOffice2007"></a>

```go
const (
    // FileFormatVersionOffice2007 targets Office 2007 format.
    FileFormatVersionOffice2007 FileFormatVersion = iota

    // FileFormatVersionOffice2010 targets Office 2010 format.
    FileFormatVersionOffice2010

    // FileFormatVersionOffice2013 targets Office 2013 format.
    FileFormatVersionOffice2013

    // FileFormatVersionOffice2016 targets Office 2016 format.
    FileFormatVersionOffice2016

    // FileFormatVersionOffice2019 targets Office 2019 format.
    FileFormatVersionOffice2019

    // FileFormatVersionOffice2021 targets Office 2021 format.
    FileFormatVersionOffice2021

    // FileFormatVersionMicrosoft365 targets Microsoft 365 format.
    FileFormatVersionMicrosoft365
)
```

<a name="FormField"></a>
## type FormField

FormField wraps a form field element in a Word document. It provides a high\-level API for working with legacy form fields \(Word 97\-2003 compatible using fldSimple/ffData\).

```go
type FormField struct {
    simpleField *elements.SimpleField
    ffData      *elements.FormFieldData
    paragraph   *elements.Paragraph
    fieldType   FormFieldType
}
```

<a name="InsertCheckBox"></a>
### func InsertCheckBox

```go
func InsertCheckBox(p *elements.Paragraph, name string, checked bool) *FormField
```

InsertCheckBox inserts a checkbox form field into the paragraph. Creates a SimpleField with FORMCHECKBOX instruction, FormFieldData, and checkbox symbol.

<a name="InsertDropDown"></a>
### func InsertDropDown

```go
func InsertDropDown(p *elements.Paragraph, name string, items []string, selectedIndex int) *FormField
```

InsertDropDown inserts a dropdown form field into the paragraph. Creates a SimpleField with FORMDROPDOWN instruction, FormFieldData, and list items. The selectedIndex parameter specifies which item is initially selected \(0\-based\).

<a name="InsertTextField"></a>
### func InsertTextField

```go
func InsertTextField(p *elements.Paragraph, name, defaultValue string) *FormField
```

InsertTextField inserts a text form field into the paragraph. Creates a SimpleField with FORMTEXT instruction, FormFieldData, and default value.

<a name="newFormField"></a>
### func newFormField

```go
func newFormField(simpleField *elements.SimpleField, ffData *elements.FormFieldData, paragraph *elements.Paragraph, fieldType FormFieldType) *FormField
```

newFormField creates a new FormField wrapper.

<a name="FormField.Enabled"></a>
### func \(\*FormField\) Enabled

```go
func (f *FormField) Enabled() bool
```

Enabled returns whether the form field is enabled.

<a name="FormField.GetCheckBoxSize"></a>
### func \(\*FormField\) GetCheckBoxSize

```go
func (f *FormField) GetCheckBoxSize() int
```

GetCheckBoxSize returns the size of the checkbox in half\-points. Returns 0 if no size is set or if this is not a checkbox field.

<a name="FormField.GetDefaultTextValue"></a>
### func \(\*FormField\) GetDefaultTextValue

```go
func (f *FormField) GetDefaultTextValue() string
```

GetDefaultTextValue returns the default value for a text field. Returns empty string if this is not a text field.

<a name="FormField.GetDropDownItems"></a>
### func \(\*FormField\) GetDropDownItems

```go
func (f *FormField) GetDropDownItems() []string
```

GetDropDownItems returns the list of items in a dropdown field. Returns empty slice if this is not a dropdown field.

<a name="FormField.GetMaxLength"></a>
### func \(\*FormField\) GetMaxLength

```go
func (f *FormField) GetMaxLength() int
```

GetMaxLength returns the maximum length for a text field. Returns 0 if no maximum or if this is not a text field.

<a name="FormField.GetSelectedIndex"></a>
### func \(\*FormField\) GetSelectedIndex

```go
func (f *FormField) GetSelectedIndex() int
```

GetSelectedIndex returns the index of the selected item in a dropdown field. Returns \-1 if no item is selected or if this is not a dropdown field.

<a name="FormField.GetSelectedValue"></a>
### func \(\*FormField\) GetSelectedValue

```go
func (f *FormField) GetSelectedValue() string
```

GetSelectedValue returns the value of the selected item in a dropdown field. Returns empty string if no item is selected or if this is not a dropdown field.

<a name="FormField.GetTextValue"></a>
### func \(\*FormField\) GetTextValue

```go
func (f *FormField) GetTextValue() string
```

GetTextValue returns the current value of a text field. Returns empty string if this is not a text field.

<a name="FormField.IsChecked"></a>
### func \(\*FormField\) IsChecked

```go
func (f *FormField) IsChecked() bool
```

IsChecked returns whether the checkbox is checked. Returns false if this is not a checkbox field.

<a name="FormField.Name"></a>
### func \(\*FormField\) Name

```go
func (f *FormField) Name() string
```

Name returns the name of the form field.

<a name="FormField.Remove"></a>
### func \(\*FormField\) Remove

```go
func (f *FormField) Remove()
```

Remove removes the form field from its paragraph.

<a name="FormField.SetCheckBoxSize"></a>
### func \(\*FormField\) SetCheckBoxSize

```go
func (f *FormField) SetCheckBoxSize(size int)
```

SetCheckBoxSize sets the size of the checkbox in half\-points. Does nothing if this is not a checkbox field.

<a name="FormField.SetChecked"></a>
### func \(\*FormField\) SetChecked

```go
func (f *FormField) SetChecked(isChecked bool)
```

SetChecked sets whether the checkbox is checked. Does nothing if this is not a checkbox field.

<a name="FormField.SetDefaultTextValue"></a>
### func \(\*FormField\) SetDefaultTextValue

```go
func (f *FormField) SetDefaultTextValue(value string)
```

SetDefaultTextValue sets the default value for a text field. Does nothing if this is not a text field.

<a name="FormField.SetDropDownItems"></a>
### func \(\*FormField\) SetDropDownItems

```go
func (f *FormField) SetDropDownItems(items []string)
```

SetDropDownItems sets the list of items in a dropdown field. Maximum 25 items are allowed per Word specification. Does nothing if this is not a dropdown field.

<a name="FormField.SetEnabled"></a>
### func \(\*FormField\) SetEnabled

```go
func (f *FormField) SetEnabled(isEnabled bool)
```

SetEnabled sets whether the form field is enabled.

<a name="FormField.SetMaxLength"></a>
### func \(\*FormField\) SetMaxLength

```go
func (f *FormField) SetMaxLength(length int)
```

SetMaxLength sets the maximum length for a text field. Does nothing if this is not a text field.

<a name="FormField.SetName"></a>
### func \(\*FormField\) SetName

```go
func (f *FormField) SetName(name string)
```

SetName sets the name of the form field.

<a name="FormField.SetSelectedIndex"></a>
### func \(\*FormField\) SetSelectedIndex

```go
func (f *FormField) SetSelectedIndex(index int)
```

SetSelectedIndex sets the index of the selected item in a dropdown field. Does nothing if this is not a dropdown field.

<a name="FormField.SetTextValue"></a>
### func \(\*FormField\) SetTextValue

```go
func (f *FormField) SetTextValue(value string)
```

SetTextValue sets the value of a text field. Does nothing if this is not a text field.

<a name="FormField.Type"></a>
### func \(\*FormField\) Type

```go
func (f *FormField) Type() FormFieldType
```

Type returns the type of the form field.

<a name="FormFieldType"></a>
## type FormFieldType

FormFieldType represents the type of form field.

```go
type FormFieldType int
```

<a name="FormFieldTypeText"></a>

```go
const (
    // FormFieldTypeText represents a text input form field (FORMTEXT).
    FormFieldTypeText FormFieldType = iota
    // FormFieldTypeCheckBox represents a checkbox form field (FORMCHECKBOX).
    FormFieldTypeCheckBox
    // FormFieldTypeDropDown represents a dropdown list form field (FORMDROPDOWN).
    FormFieldTypeDropDown
)
```

<a name="detectFormFieldType"></a>
### func detectFormFieldType

```go
func detectFormFieldType(instruction string) (FormFieldType, error)
```

detectFormFieldType detects the type of form field from the instruction.

<a name="FormFieldType.String"></a>
### func \(FormFieldType\) String

```go
func (t FormFieldType) String() string
```

String returns the string representation of the form field type.

<a name="ImageBuilder"></a>
## type ImageBuilder

ImageBuilder provides a fluent API for adding images.

```go
type ImageBuilder struct {
    docBuilder  *DocumentBuilder
    data        []byte
    contentType string
    width       int64
    height      int64
    inline      bool
}
```

<a name="ImageBuilder.Document"></a>
### func \(\*ImageBuilder\) Document

```go
func (ib *ImageBuilder) Document() *DocumentBuilder
```

Document returns the DocumentBuilder without inserting.

<a name="ImageBuilder.Height"></a>
### func \(\*ImageBuilder\) Height

```go
func (ib *ImageBuilder) Height(emus int64) *ImageBuilder
```

Height sets the image height in EMUs.

<a name="ImageBuilder.HeightCm"></a>
### func \(\*ImageBuilder\) HeightCm

```go
func (ib *ImageBuilder) HeightCm(cm float64) *ImageBuilder
```

HeightCm sets the image height in centimeters.

<a name="ImageBuilder.HeightInches"></a>
### func \(\*ImageBuilder\) HeightInches

```go
func (ib *ImageBuilder) HeightInches(inches float64) *ImageBuilder
```

HeightInches sets the image height in inches.

<a name="ImageBuilder.Inline"></a>
### func \(\*ImageBuilder\) Inline

```go
func (ib *ImageBuilder) Inline() *ImageBuilder
```

Inline makes the image inline \(default\).

<a name="ImageBuilder.Insert"></a>
### func \(\*ImageBuilder\) Insert

```go
func (ib *ImageBuilder) Insert() *DocumentBuilder
```

Insert inserts the image and returns to the DocumentBuilder. Note: The actual image part creation happens in Build\(\) when a package is available.

<a name="ImageBuilder.Size"></a>
### func \(\*ImageBuilder\) Size

```go
func (ib *ImageBuilder) Size(width, height int64) *ImageBuilder
```

Size sets both width and height in EMUs.

<a name="ImageBuilder.SizeCm"></a>
### func \(\*ImageBuilder\) SizeCm

```go
func (ib *ImageBuilder) SizeCm(width, height float64) *ImageBuilder
```

SizeCm sets both width and height in centimeters.

<a name="ImageBuilder.SizeInches"></a>
### func \(\*ImageBuilder\) SizeInches

```go
func (ib *ImageBuilder) SizeInches(width, height float64) *ImageBuilder
```

SizeInches sets both width and height in inches.

<a name="ImageBuilder.Width"></a>
### func \(\*ImageBuilder\) Width

```go
func (ib *ImageBuilder) Width(emus int64) *ImageBuilder
```

Width sets the image width in EMUs.

<a name="ImageBuilder.WidthCm"></a>
### func \(\*ImageBuilder\) WidthCm

```go
func (ib *ImageBuilder) WidthCm(cm float64) *ImageBuilder
```

WidthCm sets the image width in centimeters.

<a name="ImageBuilder.WidthInches"></a>
### func \(\*ImageBuilder\) WidthInches

```go
func (ib *ImageBuilder) WidthInches(inches float64) *ImageBuilder
```

WidthInches sets the image width in inches.

<a name="MarkupCompatibilityProcessSettings"></a>
## type MarkupCompatibilityProcessSettings

MarkupCompatibilityProcessSettings controls markup compatibility processing.

```go
type MarkupCompatibilityProcessSettings struct {
    // ProcessModeValues determines which mc:ProcessContent elements to process.
    ProcessModeValues ProcessMode

    // TargetFileFormatVersions specifies which Office versions to target.
    TargetFileFormatVersions FileFormatVersion
}
```

<a name="OpenSettings"></a>
## type OpenSettings

OpenSettings contains options for opening or creating documents.

```go
type OpenSettings struct {
    // AutoSave determines if the document should be automatically saved
    // when Close() is called and there are unsaved changes.
    AutoSave bool

    // MaxCharactersInPart sets the maximum number of characters allowed
    // in a single part. A value of 0 means no limit.
    MaxCharactersInPart int64

    // MarkupCompatibilityProcessSettings controls how markup compatibility
    // elements are processed. Currently not implemented.
    MarkupCompatibilityProcessSettings MarkupCompatibilityProcessSettings
}
```

<a name="DefaultOpenSettings"></a>
### func DefaultOpenSettings

```go
func DefaultOpenSettings() *OpenSettings
```

DefaultOpenSettings returns the default settings for opening documents.

<a name="OpenSettings.WithAutoSave"></a>
### func \(\*OpenSettings\) WithAutoSave

```go
func (s *OpenSettings) WithAutoSave(autoSave bool) *OpenSettings
```

WithAutoSave returns a copy of the settings with AutoSave enabled.

<a name="OpenSettings.WithMaxCharacters"></a>
### func \(\*OpenSettings\) WithMaxCharacters

```go
func (s *OpenSettings) WithMaxCharacters(maxChars int64) *OpenSettings
```

WithMaxCharacters returns a copy of the settings with the specified max.

<a name="OpenSettings.WithTargetVersion"></a>
### func \(\*OpenSettings\) WithTargetVersion

```go
func (s *OpenSettings) WithTargetVersion(version FileFormatVersion) *OpenSettings
```

WithTargetVersion returns a copy of the settings with the target version.

<a name="ParagraphBuilder"></a>
## type ParagraphBuilder

ParagraphBuilder provides a fluent API for building paragraphs.

```go
type ParagraphBuilder struct {
    paragraph  *elements.Paragraph
    docBuilder *DocumentBuilder
}
```

<a name="ParagraphBuilder.AddLineBreak"></a>
### func \(\*ParagraphBuilder\) AddLineBreak

```go
func (pb *ParagraphBuilder) AddLineBreak() *ParagraphBuilder
```

AddLineBreak adds a line break to the paragraph.

<a name="ParagraphBuilder.AddRun"></a>
### func \(\*ParagraphBuilder\) AddRun

```go
func (pb *ParagraphBuilder) AddRun(text string) *RunBuilder
```

AddRun adds a new run with the given text and returns a RunBuilder.

<a name="ParagraphBuilder.Align"></a>
### func \(\*ParagraphBuilder\) Align

```go
func (pb *ParagraphBuilder) Align(alignment elements.JustificationValue) *ParagraphBuilder
```

Align sets the paragraph alignment.

<a name="ParagraphBuilder.AlignCenter"></a>
### func \(\*ParagraphBuilder\) AlignCenter

```go
func (pb *ParagraphBuilder) AlignCenter() *ParagraphBuilder
```

AlignCenter centers the paragraph.

<a name="ParagraphBuilder.AlignJustify"></a>
### func \(\*ParagraphBuilder\) AlignJustify

```go
func (pb *ParagraphBuilder) AlignJustify() *ParagraphBuilder
```

AlignJustify justifies the paragraph.

<a name="ParagraphBuilder.AlignLeft"></a>
### func \(\*ParagraphBuilder\) AlignLeft

```go
func (pb *ParagraphBuilder) AlignLeft() *ParagraphBuilder
```

AlignLeft aligns the paragraph to the left.

<a name="ParagraphBuilder.AlignRight"></a>
### func \(\*ParagraphBuilder\) AlignRight

```go
func (pb *ParagraphBuilder) AlignRight() *ParagraphBuilder
```

AlignRight aligns the paragraph to the right.

<a name="ParagraphBuilder.Bold"></a>
### func \(\*ParagraphBuilder\) Bold

```go
func (pb *ParagraphBuilder) Bold() *ParagraphBuilder
```

Bold makes all text in the paragraph bold.

<a name="ParagraphBuilder.Color"></a>
### func \(\*ParagraphBuilder\) Color

```go
func (pb *ParagraphBuilder) Color(hex string) *ParagraphBuilder
```

Color sets the text color for all text in the paragraph. Color should be a hex value without \# \(e.g., "FF0000" for red\).

<a name="ParagraphBuilder.Document"></a>
### func \(\*ParagraphBuilder\) Document

```go
func (pb *ParagraphBuilder) Document() *DocumentBuilder
```

Document returns the DocumentBuilder to continue building.

<a name="ParagraphBuilder.FirstLineIndent"></a>
### func \(\*ParagraphBuilder\) FirstLineIndent

```go
func (pb *ParagraphBuilder) FirstLineIndent(points int) *ParagraphBuilder
```

FirstLineIndent sets the first line indentation in points.

<a name="ParagraphBuilder.Font"></a>
### func \(\*ParagraphBuilder\) Font

```go
func (pb *ParagraphBuilder) Font(name string, size int) *ParagraphBuilder
```

Font sets the font name and size for all text in the paragraph. Size is in points \(e.g., 12 for 12pt\).

<a name="ParagraphBuilder.FontSize"></a>
### func \(\*ParagraphBuilder\) FontSize

```go
func (pb *ParagraphBuilder) FontSize(size int) *ParagraphBuilder
```

FontSize sets the font size for all text in the paragraph. Size is in points \(e.g., 12 for 12pt\).

<a name="ParagraphBuilder.HangingIndent"></a>
### func \(\*ParagraphBuilder\) HangingIndent

```go
func (pb *ParagraphBuilder) HangingIndent(points int) *ParagraphBuilder
```

HangingIndent sets the hanging indentation in points.

<a name="ParagraphBuilder.Highlight"></a>
### func \(\*ParagraphBuilder\) Highlight

```go
func (pb *ParagraphBuilder) Highlight(color elements.HighlightColor) *ParagraphBuilder
```

Highlight sets the highlight color for all text in the paragraph.

<a name="ParagraphBuilder.Italic"></a>
### func \(\*ParagraphBuilder\) Italic

```go
func (pb *ParagraphBuilder) Italic() *ParagraphBuilder
```

Italic makes all text in the paragraph italic.

<a name="ParagraphBuilder.LeftIndent"></a>
### func \(\*ParagraphBuilder\) LeftIndent

```go
func (pb *ParagraphBuilder) LeftIndent(points int) *ParagraphBuilder
```

LeftIndent sets the left indentation in points.

<a name="ParagraphBuilder.Paragraph"></a>
### func \(\*ParagraphBuilder\) Paragraph

```go
func (pb *ParagraphBuilder) Paragraph() *elements.Paragraph
```

Paragraph returns the underlying Paragraph element.

<a name="ParagraphBuilder.RightIndent"></a>
### func \(\*ParagraphBuilder\) RightIndent

```go
func (pb *ParagraphBuilder) RightIndent(points int) *ParagraphBuilder
```

RightIndent sets the right indentation in points.

<a name="ParagraphBuilder.SpacingAfter"></a>
### func \(\*ParagraphBuilder\) SpacingAfter

```go
func (pb *ParagraphBuilder) SpacingAfter(points int) *ParagraphBuilder
```

SpacingAfter sets the space after the paragraph in points.

<a name="ParagraphBuilder.SpacingBefore"></a>
### func \(\*ParagraphBuilder\) SpacingBefore

```go
func (pb *ParagraphBuilder) SpacingBefore(points int) *ParagraphBuilder
```

SpacingBefore sets the space before the paragraph in points.

<a name="ParagraphBuilder.Style"></a>
### func \(\*ParagraphBuilder\) Style

```go
func (pb *ParagraphBuilder) Style(styleId string) *ParagraphBuilder
```

Style sets the paragraph style.

<a name="ParagraphBuilder.Underline"></a>
### func \(\*ParagraphBuilder\) Underline

```go
func (pb *ParagraphBuilder) Underline() *ParagraphBuilder
```

Underline adds underline to all text in the paragraph.

<a name="ProcessMode"></a>
## type ProcessMode

ProcessMode specifies how to process markup compatibility content.

```go
type ProcessMode int
```

<a name="ProcessModeNoProcess"></a>

```go
const (
    // ProcessModeNoProcess does not process mc:ProcessContent.
    ProcessModeNoProcess ProcessMode = iota

    // ProcessModeProcessLoadedPartsOnly processes only loaded parts.
    ProcessModeProcessLoadedPartsOnly

    // ProcessModeProcessAllParts processes all parts.
    ProcessModeProcessAllParts
)
```

<a name="Revision"></a>
## type Revision

Revision wraps a revision tracking element and provides unified access to its properties.

```go
type Revision struct {
    // element is the underlying revision element (InsertedRun, DeletedRun, etc.).
    element any

    // typ is the type of this revision.
    typ RevisionType

    // parent is the parent OpenXML element.
    parent openxml.Element

    // document is the document this revision belongs to.
    document *Document
}
```

<a name="collectRevisionsFromElement"></a>
### func collectRevisionsFromElement

```go
func collectRevisionsFromElement(elem openxml.Element, doc *Document) []*Revision
```

collectRevisionsFromElement recursively walks the element tree and collects all revisions. It detects InsertedRun, DeletedRun, MoveFromRun, MoveToRun, RunPropertiesChange, and ParagraphPropertiesChange elements and wraps them in Revision structs.

<a name="newRevision"></a>
### func newRevision

```go
func newRevision(element any, parent openxml.Element, doc *Document) *Revision
```

newRevision creates a new Revision wrapper for the given element. It automatically detects the revision type based on the element type.

<a name="Revision.Accept"></a>
### func \(\*Revision\) Accept

```go
func (r *Revision) Accept() error
```

Accept accepts this revision, applying the change to the document. The behavior depends on the revision type: \- Insert: Replace the InsertedRun with its Run children \- Delete: Remove the DeletedRun \(content stays deleted\) \- MoveFrom/MoveTo: Returns error \(requires move pair coordination\) \- FormatChange: Remove the change wrapper, keep current properties

<a name="Revision.Author"></a>
### func \(\*Revision\) Author

```go
func (r *Revision) Author() string
```

Author returns the author who made this revision.

<a name="Revision.Content"></a>
### func \(\*Revision\) Content

```go
func (r *Revision) Content() string
```

Content returns the text content of this revision. For insertions and moves, this returns the inserted/moved text. For deletions, this returns the deleted text. For format changes, this returns an empty string.

<a name="Revision.Date"></a>
### func \(\*Revision\) Date

```go
func (r *Revision) Date() time.Time
```

Date returns the date/time when this revision was made.

<a name="Revision.Document"></a>
### func \(\*Revision\) Document

```go
func (r *Revision) Document() *Document
```

Document returns the document this revision belongs to.

<a name="Revision.Element"></a>
### func \(\*Revision\) Element

```go
func (r *Revision) Element() any
```

Element returns the underlying revision element.

<a name="Revision.Id"></a>
### func \(\*Revision\) Id

```go
func (r *Revision) Id() int
```

Id returns the revision ID.

<a name="Revision.Parent"></a>
### func \(\*Revision\) Parent

```go
func (r *Revision) Parent() openxml.Element
```

Parent returns the parent OpenXML element.

<a name="Revision.Reject"></a>
### func \(\*Revision\) Reject

```go
func (r *Revision) Reject() error
```

Reject rejects this revision, reverting the change. The behavior depends on the revision type: \- Insert: Remove the InsertedRun and all content \- Delete: Convert DeletedText to regular Text, restore content \- MoveFrom/MoveTo: Returns error \(requires move pair coordination\) \- FormatChange: Restore previous properties from the change wrapper

<a name="Revision.Type"></a>
### func \(\*Revision\) Type

```go
func (r *Revision) Type() RevisionType
```

Type returns the type of this revision.

<a name="Revision.acceptDeletedRun"></a>
### func \(\*Revision\) acceptDeletedRun

```go
func (r *Revision) acceptDeletedRun() error
```

acceptDeletedRun removes the DeletedRun \(content stays deleted\).

<a name="Revision.acceptFormatChange"></a>
### func \(\*Revision\) acceptFormatChange

```go
func (r *Revision) acceptFormatChange() error
```

acceptFormatChange removes the change wrapper, keeping current properties.

<a name="Revision.acceptInsertedRun"></a>
### func \(\*Revision\) acceptInsertedRun

```go
func (r *Revision) acceptInsertedRun() error
```

acceptInsertedRun replaces the InsertedRun with its Run children.

<a name="Revision.acceptMoveRun"></a>
### func \(\*Revision\) acceptMoveRun

```go
func (*Revision) acceptMoveRun() error
```

acceptMoveRun returns an error indicating move operations are not yet supported.

<a name="Revision.convertDeletedTextsInRun"></a>
### func \(\*Revision\) convertDeletedTextsInRun

```go
func (*Revision) convertDeletedTextsInRun(run *elements.Run)
```

convertDeletedTextsInRun converts all DeletedText elements to regular Text.

<a name="Revision.rejectDeletedRun"></a>
### func \(\*Revision\) rejectDeletedRun

```go
func (r *Revision) rejectDeletedRun() error
```

rejectDeletedRun converts DeletedText to regular Text and restores content.

<a name="Revision.rejectFormatChange"></a>
### func \(\*Revision\) rejectFormatChange

```go
func (r *Revision) rejectFormatChange() error
```

rejectFormatChange extracts previous properties and restores them.

<a name="Revision.rejectInsertedRun"></a>
### func \(\*Revision\) rejectInsertedRun

```go
func (r *Revision) rejectInsertedRun() error
```

rejectInsertedRun removes the InsertedRun and all content.

<a name="Revision.rejectMoveRun"></a>
### func \(\*Revision\) rejectMoveRun

```go
func (*Revision) rejectMoveRun() error
```

rejectMoveRun returns an error indicating move operations are not yet supported.

<a name="Revision.rejectParagraphPropertiesChange"></a>
### func \(\*Revision\) rejectParagraphPropertiesChange

```go
func (*Revision) rejectParagraphPropertiesChange(parent openxml.CompositeElement, change *elements.ParagraphPropertiesChange)
```

rejectParagraphPropertiesChange restores previous paragraph properties.

<a name="Revision.rejectRunPropertiesChange"></a>
### func \(\*Revision\) rejectRunPropertiesChange

```go
func (*Revision) rejectRunPropertiesChange(parent openxml.CompositeElement, change *elements.RunPropertiesChange)
```

rejectRunPropertiesChange restores previous run properties.

<a name="RevisionCollection"></a>
## type RevisionCollection

RevisionCollection provides filtering and querying capabilities for revisions.

```go
type RevisionCollection struct {
    revisions []*Revision
}
```

<a name="NewRevisionCollection"></a>
### func NewRevisionCollection

```go
func NewRevisionCollection(revisions []*Revision) *RevisionCollection
```

NewRevisionCollection creates a new RevisionCollection from a slice of revisions.

<a name="RevisionCollection.All"></a>
### func \(\*RevisionCollection\) All

```go
func (rc *RevisionCollection) All() []*Revision
```

All returns all revisions in the collection.

<a name="RevisionCollection.ByAuthor"></a>
### func \(\*RevisionCollection\) ByAuthor

```go
func (rc *RevisionCollection) ByAuthor(author string) []*Revision
```

ByAuthor filters revisions by author name.

<a name="RevisionCollection.ByType"></a>
### func \(\*RevisionCollection\) ByType

```go
func (rc *RevisionCollection) ByType(typ RevisionType) []*Revision
```

ByType filters revisions by type.

<a name="RevisionCollection.Count"></a>
### func \(\*RevisionCollection\) Count

```go
func (rc *RevisionCollection) Count() int
```

Count returns the number of revisions in the collection.

<a name="RevisionType"></a>
## type RevisionType

RevisionType represents the type of a tracked change.

```go
type RevisionType int
```

<a name="RevisionTypeInsert"></a>

```go
const (
    // RevisionTypeInsert represents an insertion revision (w:ins).
    RevisionTypeInsert RevisionType = iota

    // RevisionTypeDelete represents a deletion revision (w:del).
    RevisionTypeDelete

    // RevisionTypeMoveFrom represents a move source revision (w:moveFrom).
    RevisionTypeMoveFrom

    // RevisionTypeMoveTo represents a move destination revision (w:moveTo).
    RevisionTypeMoveTo

    // RevisionTypeFormatChange represents a formatting change revision
    // (w:rPrChange or w:pPrChange).
    RevisionTypeFormatChange
)
```

<a name="RevisionType.String"></a>
### func \(RevisionType\) String

```go
func (rt RevisionType) String() string
```

String returns the string representation of the revision type.

<a name="RunBuilder"></a>
## type RunBuilder

RunBuilder provides a fluent API for building runs.

```go
type RunBuilder struct {
    run              *elements.Run
    paragraphBuilder *ParagraphBuilder
}
```

<a name="RunBuilder.AddBreak"></a>
### func \(\*RunBuilder\) AddBreak

```go
func (rb *RunBuilder) AddBreak(breakType elements.BreakType) *RunBuilder
```

AddBreak adds a break to this run.

<a name="RunBuilder.AddTab"></a>
### func \(\*RunBuilder\) AddTab

```go
func (rb *RunBuilder) AddTab() *RunBuilder
```

AddTab adds a tab to this run.

<a name="RunBuilder.AddText"></a>
### func \(\*RunBuilder\) AddText

```go
func (rb *RunBuilder) AddText(text string) *RunBuilder
```

AddText adds more text to this run.

<a name="RunBuilder.Bold"></a>
### func \(\*RunBuilder\) Bold

```go
func (rb *RunBuilder) Bold() *RunBuilder
```

Bold makes this run bold.

<a name="RunBuilder.Color"></a>
### func \(\*RunBuilder\) Color

```go
func (rb *RunBuilder) Color(hex string) *RunBuilder
```

Color sets the text color \(hex without \#\).

<a name="RunBuilder.Document"></a>
### func \(\*RunBuilder\) Document

```go
func (rb *RunBuilder) Document() *DocumentBuilder
```

Document returns the DocumentBuilder to continue building the document.

<a name="RunBuilder.Font"></a>
### func \(\*RunBuilder\) Font

```go
func (rb *RunBuilder) Font(name string) *RunBuilder
```

Font sets the font name.

<a name="RunBuilder.FontSize"></a>
### func \(\*RunBuilder\) FontSize

```go
func (rb *RunBuilder) FontSize(points int) *RunBuilder
```

FontSize sets the font size in points.

<a name="RunBuilder.Highlight"></a>
### func \(\*RunBuilder\) Highlight

```go
func (rb *RunBuilder) Highlight(color elements.HighlightColor) *RunBuilder
```

Highlight sets the highlight color.

<a name="RunBuilder.Italic"></a>
### func \(\*RunBuilder\) Italic

```go
func (rb *RunBuilder) Italic() *RunBuilder
```

Italic makes this run italic.

<a name="RunBuilder.Paragraph"></a>
### func \(\*RunBuilder\) Paragraph

```go
func (rb *RunBuilder) Paragraph() *ParagraphBuilder
```

Paragraph returns the ParagraphBuilder to continue building the paragraph.

<a name="RunBuilder.Run"></a>
### func \(\*RunBuilder\) Run

```go
func (rb *RunBuilder) Run() *elements.Run
```

Run returns the underlying Run element.

<a name="RunBuilder.Underline"></a>
### func \(\*RunBuilder\) Underline

```go
func (rb *RunBuilder) Underline() *RunBuilder
```

Underline adds underline to this run.

<a name="RunBuilder.UnderlineStyle"></a>
### func \(\*RunBuilder\) UnderlineStyle

```go
func (rb *RunBuilder) UnderlineStyle(style elements.UnderlineValue) *RunBuilder
```

UnderlineStyle sets a specific underline style.

<a name="TableBuilder"></a>
## type TableBuilder

TableBuilder provides a fluent API for building tables.

```go
type TableBuilder struct {
    table      *elements.Table
    docBuilder *DocumentBuilder
}
```

<a name="TableBuilder.AddRow"></a>
### func \(\*TableBuilder\) AddRow

```go
func (tb *TableBuilder) AddRow() *TableBuilder
```

AddRow appends a new row with the same number of columns as the table.

<a name="TableBuilder.Cell"></a>
### func \(\*TableBuilder\) Cell

```go
func (tb *TableBuilder) Cell(row, col int) *TableCellBuilder
```

Cell returns a TableCellBuilder for the specified cell.

<a name="TableBuilder.Document"></a>
### func \(\*TableBuilder\) Document

```go
func (tb *TableBuilder) Document() *DocumentBuilder
```

Document returns the DocumentBuilder to continue building.

<a name="TableBuilder.Row"></a>
### func \(\*TableBuilder\) Row

```go
func (tb *TableBuilder) Row(index int) *TableRowBuilder
```

Row returns a TableRowBuilder for the specified row.

<a name="TableBuilder.SetBorders"></a>
### func \(\*TableBuilder\) SetBorders

```go
func (tb *TableBuilder) SetBorders(style elements.BorderStyle, size int, color string) *TableBuilder
```

SetBorders sets all table borders.

<a name="TableBuilder.SetCellText"></a>
### func \(\*TableBuilder\) SetCellText

```go
func (tb *TableBuilder) SetCellText(row, col int, text string) *TableBuilder
```

SetCellText sets the text content of a specific cell.

<a name="TableBuilder.SetColumnWidth"></a>
### func \(\*TableBuilder\) SetColumnWidth

```go
func (tb *TableBuilder) SetColumnWidth(col int, width int) *TableBuilder
```

SetColumnWidth sets the width of a specific column in twips.

<a name="TableBuilder.SetColumnWidthInches"></a>
### func \(\*TableBuilder\) SetColumnWidthInches

```go
func (tb *TableBuilder) SetColumnWidthInches(col int, inches float64) *TableBuilder
```

SetColumnWidthInches sets the width of a specific column in inches.

<a name="TableBuilder.SetStyle"></a>
### func \(\*TableBuilder\) SetStyle

```go
func (tb *TableBuilder) SetStyle(styleId string) *TableBuilder
```

SetStyle sets the table style.

<a name="TableBuilder.SetWidth"></a>
### func \(\*TableBuilder\) SetWidth

```go
func (tb *TableBuilder) SetWidth(width int, widthType elements.TableWidthType) *TableBuilder
```

SetWidth sets the table width.

<a name="TableBuilder.SetWidthPercent"></a>
### func \(\*TableBuilder\) SetWidthPercent

```go
func (tb *TableBuilder) SetWidthPercent(percent int) *TableBuilder
```

SetWidthPercent sets the table width as a percentage.

<a name="TableBuilder.Table"></a>
### func \(\*TableBuilder\) Table

```go
func (tb *TableBuilder) Table() *elements.Table
```

Table returns the underlying Table element.

<a name="TableCellBuilder"></a>
## type TableCellBuilder

TableCellBuilder provides a fluent API for building table cells.

```go
type TableCellBuilder struct {
    cell         *elements.TableCell
    tableBuilder *TableBuilder
}
```

<a name="TableCellBuilder.AddParagraph"></a>
### func \(\*TableCellBuilder\) AddParagraph

```go
func (tcb *TableCellBuilder) AddParagraph(text string) *ParagraphBuilder
```

AddParagraph adds a paragraph to the cell.

<a name="TableCellBuilder.Cell"></a>
### func \(\*TableCellBuilder\) Cell

```go
func (tcb *TableCellBuilder) Cell() *elements.TableCell
```

Cell returns the underlying TableCell element.

<a name="TableCellBuilder.SetBorders"></a>
### func \(\*TableCellBuilder\) SetBorders

```go
func (tcb *TableCellBuilder) SetBorders(style elements.BorderStyle, size int, color string) *TableCellBuilder
```

SetBorders sets all cell borders.

<a name="TableCellBuilder.SetHorizontalMerge"></a>
### func \(\*TableCellBuilder\) SetHorizontalMerge

```go
func (tcb *TableCellBuilder) SetHorizontalMerge(span int) *TableCellBuilder
```

SetHorizontalMerge sets horizontal cell span \(gridSpan\).

<a name="TableCellBuilder.SetShading"></a>
### func \(\*TableCellBuilder\) SetShading

```go
func (tcb *TableCellBuilder) SetShading(fillColor string) *TableCellBuilder
```

SetShading sets the cell background color.

<a name="TableCellBuilder.SetText"></a>
### func \(\*TableCellBuilder\) SetText

```go
func (tcb *TableCellBuilder) SetText(text string) *TableCellBuilder
```

SetText sets the cell text content.

<a name="TableCellBuilder.SetVerticalMerge"></a>
### func \(\*TableCellBuilder\) SetVerticalMerge

```go
func (tcb *TableCellBuilder) SetVerticalMerge(mergeType elements.VerticalMergeType) *TableCellBuilder
```

SetVerticalMerge sets vertical cell merge.

<a name="TableCellBuilder.SetWidth"></a>
### func \(\*TableCellBuilder\) SetWidth

```go
func (tcb *TableCellBuilder) SetWidth(width int, widthType elements.TableWidthType) *TableCellBuilder
```

SetWidth sets the cell width.

<a name="TableCellBuilder.Table"></a>
### func \(\*TableCellBuilder\) Table

```go
func (tcb *TableCellBuilder) Table() *TableBuilder
```

Table returns the TableBuilder to continue building the table.

<a name="TableRowBuilder"></a>
## type TableRowBuilder

TableRowBuilder provides a fluent API for building table rows.

```go
type TableRowBuilder struct {
    row          *elements.TableRow
    tableBuilder *TableBuilder
}
```

<a name="TableRowBuilder.Cell"></a>
### func \(\*TableRowBuilder\) Cell

```go
func (t *TableRowBuilder) Cell(col int) *TableCellBuilder
```

Cell returns a TableCellBuilder for the specified cell in this row.

<a name="TableRowBuilder.Row"></a>
### func \(\*TableRowBuilder\) Row

```go
func (t *TableRowBuilder) Row() *elements.TableRow
```

Row returns the underlying TableRow element.

<a name="TableRowBuilder.SetHeaderRow"></a>
### func \(\*TableRowBuilder\) SetHeaderRow

```go
func (t *TableRowBuilder) SetHeaderRow(isHeader bool) *TableRowBuilder
```

SetHeaderRow marks this row as a header row.

<a name="TableRowBuilder.SetHeight"></a>
### func \(\*TableRowBuilder\) SetHeight

```go
func (t *TableRowBuilder) SetHeight(height int, rule elements.HeightRule) *TableRowBuilder
```

SetHeight sets the row height.

<a name="TableRowBuilder.Table"></a>
### func \(\*TableRowBuilder\) Table

```go
func (t *TableRowBuilder) Table() *TableBuilder
```

Table returns the TableBuilder to continue building the table.

<a name="docError"></a>
## type docError



```go
type docError string
```

<a name="docError.Error"></a>
### func \(docError\) Error

```go
func (e docError) Error() string
```



<a name="imageData"></a>
## type imageData

imageData holds image data to be added to the document.

```go
type imageData struct {
    data        []byte
    contentType string
    width       int64
    height      int64
}
```

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
